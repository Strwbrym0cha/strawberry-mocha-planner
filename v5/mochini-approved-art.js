import{FACE_ATLAS,MODE_ATLAS,FACE_KEYS,FACE_ANCHORS,contextMode,normalizeFace}from'./mochini-atlas-rig.js?v=6.5.0-approved-art-atlases';

const faceIndex=Object.fromEntries(FACE_KEYS.map((key,index)=>[key,index]));
let ready=false;
const load=url=>new Promise(resolve=>{const img=new Image();img.onload=()=>resolve(true);img.onerror=()=>resolve(false);img.src=url});
const faceStyle=(el,mood)=>{if(!el)return;const key=normalizeFace(mood),i=faceIndex[key],col=i%6,row=Math.floor(i/6);el.dataset.face=key;el.style.backgroundImage=`url(${FACE_ATLAS})`;el.style.backgroundSize='600% 500%';el.style.backgroundPosition=`${col*20}% ${row*25}%`};
const modeStyle=(el,mode)=>{if(!el)return;const order=['home','daily','schedule','work','money','study','pings','gig','career','settings'],key=order.includes(mode)?mode:'home',i=order.indexOf(key),col=i%5,row=Math.floor(i/5),anchor=FACE_ANCHORS[key]||FACE_ANCHORS.home;el.dataset.mode=key;el.style.backgroundImage=`url(${MODE_ATLAS})`;el.style.backgroundSize='500% 200%';el.style.backgroundPosition=`${col*25}% ${row*100}%`;el.parentElement?.style.setProperty('--mc-face-x',`${anchor[0]*100}%`);el.parentElement?.style.setProperty('--mc-face-y',`${anchor[1]*100}%`)};
function heroMood(){return document.querySelector('.mochini-hero[data-mochini-life]')?.dataset.mochiniMood||'content'}
function upgradeHero(){const live=document.querySelector('[data-mochini-live]');if(!live)return;let face=live.querySelector('.mochini-approved-face');if(!face){face=document.createElement('div');face.className='mochini-approved-face';live.querySelector('.mochini-art-wrap')?.append(face)}live.classList.add('has-approved-art');faceStyle(face,live.dataset.mood||heroMood())}
function companionMode(root){const activity=root.querySelector('.mc-bubble-top small')?.textContent||'',classes=root.className;if(/driving/.test(classes))return'gig';if(/studying|worm-research/.test(classes))return'study';if(/working/.test(classes))return'work';if(/money/.test(classes))return'money';if(/calendar/.test(classes))return'schedule';if(/daily/.test(classes))return'daily';if(/settings/.test(classes))return'settings';return contextMode('home',activity)}
function upgradeCompanion(){const root=document.querySelector('[data-mc-root]'),body=root?.querySelector('.mc-body');if(!root||!body)return;let rig=body.querySelector('.mc-approved-rig');if(!rig){rig=document.createElement('div');rig.className='mc-approved-rig';rig.innerHTML='<div class="mc-approved-mode" data-approved-mode></div><div class="mc-approved-face" data-approved-face></div>';body.prepend(rig)}const mode=companionMode(root);modeStyle(rig.querySelector('[data-approved-mode]'),mode);faceStyle(rig.querySelector('[data-approved-face]'),root.dataset.mood||'content');root.classList.add('has-approved-art')}
function sync(){if(!ready)return;upgradeHero();upgradeCompanion()}
Promise.all([load(FACE_ATLAS),load(MODE_ATLAS)]).then(([faces,modes])=>{ready=faces&&modes;if(ready)sync()});
window.addEventListener('katos:rendered',()=>setTimeout(sync,0));window.addEventListener('katos:mochini',()=>setTimeout(sync,35));window.addEventListener('focus',sync);
