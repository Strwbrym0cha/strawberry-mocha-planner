const list=v=>Array.isArray(v)?v:[];const text=v=>String(v??'').toLowerCase();export function archiveEntries(state){const out=[],push=(kind,id,title,date,status,detail={})=>out.push({key:`${kind}:${id}`,kind,id:String(id||''),title:String(title||kind),date:String(date||''),status:String(status||''),detail});list(state.life?.tasks).filter(x=>x.done).forEach(x=>push('task',x.id,x.text,x.completedAt||x.updatedAt,'complete'));list(state.life?.threads).filter(x=>['complete','archived'].includes(x.status)).forEach(x=>push('thread',x.id,x.title,x.updatedAt||x.createdAt,x.status));list(state.growth?.goals).filter(x=>x.status==='complete').forEach(x=>push('goal',x.id,x.title,x.completedAt||x.updatedAt,'complete'));list(state.growth?.wins).forEach(x=>push('win',x.id,x.title,x.createdAt||x.date,'saved'));list(state.education?.sessions).forEach(x=>push('study',x.id,`${x.minutes||0}m study session`,x.completedAt,'logged'));list(state.movement?.sessions).forEach(x=>push('movement',x.id,x.name||x.title||'Movement session',x.completedAt||x.date||x.createdAt,'logged'));list(state.insights?.resetSessions).filter(x=>x.completedAt).forEach(x=>push('reset',x.id,'Soft Reset',x.completedAt,'complete'));list(state.insights?.activityLog).forEach(x=>push('activity',x.id,x.detail?.title||x.type,x.timestamp,'event',{type:x.type,source:x.source}));return out.sort((a,b)=>String(b.date).localeCompare(String(a.date)))}export function searchArchive(state,{query='',kind='all'}={}){const q=text(query);return archiveEntries(state).filter(x=>(kind==='all'||x.kind===kind)&&(!q||text(`${x.title} ${x.kind} ${x.status} ${JSON.stringify(x.detail)}`).includes(q)))}export function archiveSummary(state){const entries=archiveEntries(state),byKind={};entries.forEach(x=>byKind[x.kind]=(byKind[x.kind]||0)+1);return{total:entries.length,byKind,latest:entries[0]||null}}