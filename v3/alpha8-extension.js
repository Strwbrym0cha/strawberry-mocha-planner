let busy=false;
function renderNoms(){const strip=document.getElementById('alpha7Strip'),home=document.querySelector('.home');if(document.getElementById('alpha8Noms'))return;if(!strip&&!home)return;const card=document.createElement('a');card.id='alpha8Noms';card.className='motion-launch-card';card.href='./noms.html?v=3.0.0-alpha.16';card.style.background='linear-gradient(135deg,#fff2df,#fff,#fff0f6)';card.innerHTML='<div class="motion-launch-icon">🍱</div><div class="alpha7-kicker">NOMS NOOK V2 · V3</div><div class="alpha7-title">Food support without duplicate food databases.</div><div class="alpha7-copy">Saved Noms, Recipes, availability, Brain-fit picks, Current Nom, Diary, Grocery Basket, and Meal Map are live.</div><div class="motion-launch-cta">Open Noms Nook →</div>';if(strip)strip.appendChild(card);else home.insertAdjacentElement('afterend',card)}function sync(){if(busy)return;busy=true;requestAnimationFrame(()=>{busy=false;renderNoms()})}const app=document.getElementById('app');if(app)new MutationObserver(sync).observe(app,{childList:true,subtree:true});sync();