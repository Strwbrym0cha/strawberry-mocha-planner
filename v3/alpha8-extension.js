let busy=false;
function renderNoms(){const strip=document.getElementById('alpha7Strip'),home=document.querySelector('.home');if(document.getElementById('alpha8Noms'))return;if(!strip&&!home)return;const card=document.createElement('a');card.id='alpha8Noms';card.className='motion-launch-card';card.href='./noms.html?v=3.0.0-alpha.8';card.style.background='linear-gradient(135deg,#fff2df,#fff,#fff0f6)';card.innerHTML='<div class="motion-launch-icon">🍱</div><div class="alpha7-kicker">NOMS NOOK · ALPHA 8</div><div class="alpha7-title">Recommendations from food that actually exists.</div><div class="alpha7-copy">My Noms, Recipes, availability, strict tags, Brain-fit picks, Current Nom, and Nom Diary are live.</div><div class="motion-launch-cta">Open Noms Nook →</div>';if(strip)strip.appendChild(card);else home.insertAdjacentElement('afterend',card)}
function sync(){if(busy)return;busy=true;requestAnimationFrame(()=>{busy=false;renderNoms()})}
const app=document.getElementById('app');if(app)new MutationObserver(sync).observe(app,{childList:true,subtree:true});sync();
