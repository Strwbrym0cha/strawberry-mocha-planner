import{loadV3State,saveV3State,V3_BUILD}from'./app/schema.js?v=3.0.0-alpha.12';
import{normalizeGrowth,addGoal,updateGoal,toggleMilestone,addWin,addExperiment,addExperimentAttempt,goalProgress,experimentSummary,derivedWins}from'./app/growth.js?v=3.0.0-alpha.12';
import{addActivityEvent}from'./app/activity.js?v=3.0.0-alpha.12';
let state=loadV3State();state={...state,growth:normalizeGrowth(state.growth)};let status='Growth Room is ready.';const app=document.getElementById('app');
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function activity(type,id,detail={}){state={...state,insights:{...state.insights,activityLog:addActivityEvent(state.insights?.activityLog,type,id,state.context,detail,'growth')}}}
function persist(msg){state=saveV3State({...state,growth:normalizeGrowth(state.growth)});status=`✓ ${msg} · ${V3_BUILD}`;window.__katOSV3=state;render()}
function parseMilestones(v){return String(v||'').split('\n').map(x=>x.trim()).filter(Boolean).map((label,i)=>({id:`milestone-${Date.now().toString(36)}-${i}`,label,done:false}))}
function renderGoals(){const goals=state.growth.goals;return`<section class="card full"><div class="head"><div><div class="ey">🌱 GOALS</div><h2>Dream → milestone → progress</h2><p>No fake streak morality. Goals can be moving, paused, or complete.</p></div><div class="count">${goals.filter(x=>x.status==='moving').length}</div></div><form id="goalForm"><div class="fields"><label class="field"><span>Goal</span><input id="goalTitle" required placeholder="Finish bachelor's"></label><label class="field"><span>Kind</span><select id="goalKind"><option value="dream">Dream</option><option value="outcome" selected>Outcome</option><option value="practice">Practice</option></select></label><label class="field"><span>Target date · optional</span><input id="goalTarget" type="date"></label><label class="field wide"><span>Why it matters · optional</span><input id="goalWhy"></label><label class="field wide"><span>Milestones · one per line</span><textarea id="goalMilestones" placeholder="Finish current course\nSubmit transfer docs\nEnroll"></textarea></label></div><div class="form-actions"><button class="btn primary">🌱 Add goal</button></div></form><div class="stack">${goals.length?goals.map(g=>{const p=goalProgress(g);return`<article class="goal-card"><div class="head"><div><b>${esc(g.title)}</b><small>${g.kind} · ${g.targetDate?`target ${g.targetDate}`:'no target date'} · ${g.status}</small></div><span class="status">${p.percent}%</span></div>${g.why?`<p>${esc(g.why)}</p>`:''}<div class="progress"><i style="width:${p.percent}%"></i></div><div class="stack">${g.milestones.map(m=>`<label class="link-option"><input type="checkbox" data-milestone="${esc(g.id)}" data-milestone-id="${esc(m.id)}" ${m.done?'checked':''}> <span>${esc(m.label)}</span></label>`).join('')||'<div class="empty">No milestones. This goal can still be completed manually.</div>'}</div><div class="actions"><button class="btn tiny" data-goal-state="${esc(g.id)}" data-next="${g.status==='paused'?'moving':'paused'}">${g.status==='paused'?'Resume':'Pause'}</button><button class="btn primary tiny" data-goal-state="${esc(g.id)}" data-next="complete">✓ Complete goal</button></div></article>`}).join(''):'<div class="empty">🌱 No growth goals yet.</div>'}</div></section>`}
function renderExperiments(){const exps=state.growth.experiments;return`<section class="card"><div class="head"><div><div class="ey">🔬 EXPERIMENTS</div><h2>Test, don’t moralize</h2><p>Try a strategy and record what actually happened.</p></div><div class="count">${exps.filter(x=>x.status==='active').length}</div></div><form id="experimentForm"><div class="fields"><label class="field wide"><span>Experiment</span><input id="expTitle" required placeholder="Treadmill during work videos"></label><label class="field wide"><span>Hypothesis</span><input id="expHypothesis" placeholder="Pairing movement with videos makes starting easier"></label></div><div class="form-actions"><button class="btn primary">🔬 Start experiment</button></div></form><div class="stack">${exps.length?exps.map(e=>{const s=experimentSummary(e);return`<article class="experiment-card"><b>${esc(e.title)}</b><small>${esc(e.hypothesis||'No hypothesis written')} · ${s.count} attempt${s.count===1?'':'s'} · ${s.worked} worked</small><div class="actions">${['worked','mixed','did-not-work','skipped'].map(r=>`<button class="btn tiny" data-attempt="${esc(e.id)}" data-result="${r}">${r}</button>`).join('')}</div></article>`}).join(''):'<div class="empty">🔬 No experiments yet.</div>'}</div></section>`}
function renderWins(){const manual=state.growth.wins,derived=derivedWins(state),all=[...manual,...derived].sort((a,b)=>String(b.date).localeCompare(String(a.date)));return`<section class="card"><div class="head"><div><div class="ey">🏆 WINS</div><h2>Progress leaves receipts</h2><p>Manual wins and selected activity-derived wins live together without duplicating the source event.</p></div><div class="count">${all.length}</div></div><form id="winForm"><div class="fields"><label class="field wide"><span>Win</span><input id="winTitle" required placeholder="Passed my exam"></label><label class="field wide"><span>Note · optional</span><input id="winNote"></label></div><div class="form-actions"><button class="btn primary">🏆 Add win</button></div></form><div class="stack">${all.slice(0,20).map(w=>`<article class="panel"><b>${w.source==='activity'?'✨':'🏆'} ${esc(w.title)}</b><small>${esc(w.date)} · ${w.source==='activity'?'derived from activity':'manual'}${w.note?` · ${esc(w.note)}`:''}</small></article>`).join('')||'<div class="empty">🏆 Wins will collect here.</div>'}</div></section>`}
function render(){app.innerHTML=`<main class="shell"><a class="back" href="./?v=3.0.0-alpha.12">← Back to KatOS V3</a><section class="hero"><div class="ey">🌱 KATOS V3 · ALPHA 11+12 MERGED · GROWTH ROOM</div><h1>Growth without the tiny courtroom.</h1><p>Goals, milestones, experiments, and wins stay connected without turning missed days into failure.</p><div class="note">${esc(status)}</div></section><div class="grid">${renderGoals()}${renderExperiments()}${renderWins()}</div></main>`;bind()}
function bind(){document.getElementById('goalForm')?.addEventListener('submit',e=>{e.preventDefault();state={...state,growth:addGoal(state.growth,{title:document.getElementById('goalTitle').value,kind:document.getElementById('goalKind').value,targetDate:document.getElementById('goalTarget').value,why:document.getElementById('goalWhy').value,milestones:parseMilestones(document.getElementById('goalMilestones').value)})};const g=state.growth.goals.at(-1);activity('goal.created',g?.id,{title:g?.title});persist('Goal added')});document.querySelectorAll('[data-milestone]').forEach(input=>input.onchange=()=>{const goal=state.growth.goals.find(g=>g.id===input.dataset.milestone),before=goal?.status;state={...state,growth:toggleMilestone(state.growth,input.dataset.milestone,input.dataset.milestoneId)};const after=state.growth.goals.find(g=>g.id===input.dataset.milestone);if(before!=='complete'&&after?.status==='complete')activity('goal.completed',after.id,{title:after.title});persist('Milestone updated')});document.querySelectorAll('[data-goal-state]').forEach(b=>b.onclick=()=>{const id=b.dataset.goalState,next=b.dataset.next,goal=state.growth.goals.find(g=>g.id===id);state={...state,growth:updateGoal(state.growth,id,{status:next,completedAt:next==='complete'?new Date().toISOString():goal?.completedAt})};if(next==='complete'&&goal?.status!=='complete')activity('goal.completed',id,{title:goal?.title});persist('Goal status updated')});document.getElementById('experimentForm')?.addEventListener('submit',e=>{e.preventDefault();state={...state,growth:addExperiment(state.growth,{title:document.getElementById('expTitle').value,hypothesis:document.getElementById('expHypothesis').value})};persist('Experiment started')});document.querySelectorAll('[data-attempt]').forEach(b=>b.onclick=()=>{state={...state,growth:addExperimentAttempt(state.growth,b.dataset.attempt,{result:b.dataset.result})};activity('experiment.attempt',b.dataset.attempt,{result:b.dataset.result});persist('Experiment attempt logged')});document.getElementById('winForm')?.addEventListener('submit',e=>{e.preventDefault();state={...state,growth:addWin(state.growth,{title:document.getElementById('winTitle').value,note:document.getElementById('winNote').value})};persist('Win added')})}
render();