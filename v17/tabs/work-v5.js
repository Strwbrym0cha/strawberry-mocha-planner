import{addGigEntry,gigTotal}from'../app/gig-work.js?v=5';const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const d=()=>new Date().toISOString().slice(0,10);function renderWorkV5({root,store,router}){const draw=()=>{const s=store.get()||{},g=s.gigWork||[],total=gigTotal(s,d().slice(0,7));root.innerHTML=`<main class="v5-work"><section><p>BOSS MODE • WORK + GIGS</p><h1>Boss Mode</h1><h2>This week’s gig total: $${total.toFixed(2)}</h2></section><section><h2>Log gig work</h2><form data-g><input name="platform" placeholder="DoorDash / Shipt" required><input name="earnings" type="number" step=".01" placeholder="Earnings" required><input name="tips" type="number" step=".01" placeholder="Tips"><input name="hours" type="number" step=".1" placeholder="Hours"><input name="miles" type="number" step=".1" placeholder="Miles"><input name="date" type="date" value="${d()}"><button>Save gig</button></form></section><section><h2>Recent gigs</h2>${g.slice().reverse().map(x=>`<p><b>${esc(x.platform)}</b> • $${(Number(x.earnings)+Number(x.tips)).toFixed(2)} • ${esc(x.date)}</p>`).join('')||'<p>No gigs logged yet.</p>'}<button data-go="worktools">Open shifts and work tools</button></section></main>`;root.querySelector('[data-g]').onsubmit=e=>{e.preventDefault();if(addGigEntry(store,Object.fromEntries(new FormData(e.target))).ok)e.target.reset()};root.querySelector('[data-go]').onclick=()=>router.go('worktools')};store.subscribe(draw);draw()}export{renderWorkV5};