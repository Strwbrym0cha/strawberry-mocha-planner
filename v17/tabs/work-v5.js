import{addGigEntry,gigTotal}from'../app/gig-work.js?v=5.1';import{WORK_DAYS,normalizeWorkSchedule,shiftsForDate,shiftLabel}from'../app/work-schedule.js?v=22.1.14-20260817';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const today=()=>new Date().toISOString().slice(0,10);const dayName=()=>new Date().toLocaleDateString('en-US',{weekday:'long'}).toLowerCase();const title=x=>x[0].toUpperCase()+x.slice(1);
function renderWorkV5({root,store,router}){const draw=()=>{const s=store.get()||{},g=s.gigWork||[],schedule=normalizeWorkSchedule(s.workSchedule),todayShifts=shiftsForDate(schedule,today()),total=gigTotal(s,new Date()),week=WORK_DAYS.map(day=>({day,shifts:schedule.weekly[day]||[]}));root.innerHTML=`<main class="v5-work"><section><p>BOSS MODE • WORK + GIGS</p><h1>Boss Mode</h1><h2>This week’s gig total: $${total.toFixed(2)}</h2></section><section><h2>Scheduled work</h2><p><b>Today:</b> ${todayShifts.length?todayShifts.map(shiftLabel).map(esc).join(' • '):'No fixed shift — flexible day.'}</p><div class="v5-work-week">${week.map(x=>`<article class="${x.day===dayName()?'today':''}"><b>${title(x.day)}</b><small>${x.shifts.length?x.shifts.map(shiftLabel).map(esc).join(' • '):'Open'}</small></article>`).join('')}</div></section><section><h2>Log gig work</h2><form data-g><input name="platform" placeholder="DoorDash / Shipt" required><input name="earnings" type="number" step=".01" placeholder="Earnings" required><input name="tips" type="number" step=".01" placeholder="Tips"><input name="hours" type="number" step=".1" placeholder="Hours"><input name="miles" type="number" step=".1" placeholder="Miles"><input name="date" type="date" value="${today()}"><button class="primary">Save gig</button></form></section><section><h2>Recent gigs</h2>${g.slice().reverse().slice(0,8).map(x=>`<p><b>${esc(x.platform)}</b> • $${(Number(x.earnings)+Number(x.tips)).toFixed(2)} • ${esc(x.date)}</p>`).join('')||'<p>No gigs logged yet.</p>'}<button data-go="worktools">Edit shifts and work projects</button></section></main>`;root.querySelector('[data-g]').onsubmit=e=>{e.preventDefault();if(addGigEntry(store,Object.fromEntries(new FormData(e.target))).ok)e.target.reset()};root.querySelector('[data-go]').onclick=()=>router.go('worktools')};store.subscribe(draw);draw()}export{renderWorkV5};