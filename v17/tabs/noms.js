import{allNoms,allPlannedMeals,allRecipes,chooseSurpriseNom,createNomsActions,emergencyNoms,groceryItems,nomsMatchingFilters,pantryItems,todayNom}from'../app/noms.js?v=22.1.18-20260817';

const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
const dateKey=()=>{const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`};
const effortLabel=value=>({"no-prep":"No prep",easy:"Easy",medium:"Medium",involved:"Involved"}[value]||'Any effort');

function installNomsStyle(){if(document.getElementById('sm-noms-style'))return;const style=document.createElement('style');style.id='sm-noms-style';style.textContent=`.sm-noms{color:#674941}.sm-noms-hero{padding:24px;border:1px solid #edcdda;border-radius:29px;background:linear-gradient(135deg,#ffe4ef,#fffdf8 52%,#edf8e8);position:relative;overflow:hidden}.sm-noms-hero:after{content:'🍓  🧊  ✦';position:absolute;right:20px;bottom:13px;font-size:23px;opacity:.72}.sm-noms-hero h1{margin:4px 0}.sm-noms-fridge{margin-top:14px;padding:18px;border:8px solid #ffdce9;border-radius:35px;background:linear-gradient(115deg,#fff6fa,#fffdfb 58%,#f2faed);box-shadow:inset 0 0 0 2px #fff,inset -13px 0 22px rgba(198,158,170,.08),0 16px 36px rgba(128,76,96,.10)}.sm-noms-fridge-top{display:flex;justify-content:space-between;gap:12px;align-items:center;padding:3px 4px 15px}.sm-noms-fridge-top h2{margin:0}.sm-noms-tile-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:11px}.sm-noms-tile{min-height:112px;padding:15px;border:1px solid #e9c8d5;border-radius:21px;background:rgba(255,255,255,.83);text-align:left;color:#674941;box-shadow:0 7px 17px rgba(174,112,139,.08)}.sm-noms-tile:hover{transform:translateY(-1px);background:#fff}.sm-noms-tile b,.sm-noms-tile small{display:block}.sm-noms-tile b{font-size:17px;margin-bottom:6px}.sm-noms-tile small{color:#967b75;line-height:1.3}.sm-noms-today{margin-top:15px;padding:18px;border:2px dashed #e99abb;border-radius:23px;background:linear-gradient(100deg,#fff0f6,#f4f9e9);display:flex;align-items:center;justify-content:space-between;gap:14px}.sm-noms-today small,.sm-noms-section small{color:#967b75}.sm-noms-today h2{margin:4px 0}.sm-noms-actions{display:flex;flex-wrap:wrap;gap:8px}.sm-noms-section{margin-top:14px;padding:20px;border:1px solid #eadbd6;border-radius:25px;background:#fffdfb}.sm-noms-section>header{display:flex;align-items:start;justify-content:space-between;gap:14px;margin-bottom:13px}.sm-noms-section h2{margin:0}.sm-noms-list{display:grid;gap:9px}.sm-noms-row{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:12px;align-items:center;padding:13px;border:1px solid #eedce4;border-radius:18px;background:#fff9fb}.sm-noms-row b,.sm-noms-row small{display:block}.sm-noms-row b{overflow-wrap:anywhere}.sm-noms-row small{margin-top:4px;line-height:1.35}.sm-noms-form{display:grid;gap:10px;margin:14px 0;padding:16px;border:1px dashed #e8a5c1;border-radius:20px;background:#fff3f8}.sm-noms-form label{display:grid;gap:5px;font-weight:800;color:#8d6470;font-size:12px}.sm-noms-form input,.sm-noms-form textarea,.sm-noms-form select{width:100%;min-height:43px;padding:9px 11px;border:1px solid #e8bdd0;border-radius:12px;background:#fff;color:#674941;font:inherit}.sm-noms-form textarea{min-height:76px;resize:vertical}.sm-noms-form-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.sm-noms-form-actions{display:flex;justify-content:flex-end;gap:8px}.sm-noms-filter{display:flex;flex-wrap:wrap;gap:8px;margin-bottom:11px}.sm-noms-filter button.active{background:#f2a4c5;color:white}.sm-noms-note{min-height:22px;margin:10px 0;color:#9a6375;font-weight:700}.sm-noms-compact{display:flex;gap:7px;flex-wrap:wrap}.sm-noms-compact button{padding:7px 9px;font-size:12px}@media(max-width:700px){.sm-noms-fridge{padding:13px;border-radius:28px}.sm-noms-tile-grid{grid-template-columns:1fr}.sm-noms-today,.sm-noms-section>header{align-items:flex-start;flex-direction:column}.sm-noms-today .sm-noms-actions,.sm-noms-section>header>button{width:100%}.sm-noms-today .sm-noms-actions button{flex:1}.sm-noms-form-grid{grid-template-columns:1fr}.sm-noms-row{grid-template-columns:1fr}.sm-noms-row .sm-noms-compact{justify-content:flex-start}}`;document.head.appendChild(style)}

function renderNoms({root,store}){
 installNomsStyle();const actions=createNomsActions(store);let view='home',form=null,filters={effort:'',tag:'',favorite:false},notice='';
 const show=result=>{notice=result?.ok?'Saved safely. 🍓':(result?.error||'That did not save.');draw()};
 const formValue=(selector,fallback='')=>root.querySelector(selector)?.value??fallback;
 const renderForm=(kind,item={})=>{
  if(!form||form.kind!==kind)return'';const isEdit=!!item.id,button=isEdit?'Save changes':'Save';
  const common=`<div class="sm-noms-form-actions"><button type="button" data-cancel-form>Cancel</button><button class="primary" type="submit">${button}</button></div>`;
  if(kind==='nom')return `<form class="sm-noms-form" data-form="nom"><label>Nom name<input name="name" required value="${esc(item.name||'')}" placeholder="Quesadillas"></label><div class="sm-noms-form-grid"><label>Type<input name="type" value="${esc(item.type||'')}" placeholder="Meal, snack…"></label><label>Effort<select name="effort"><option value="">Any effort</option>${['no-prep','easy','medium','involved'].map(value=>`<option value="${value}" ${item.effort===value?'selected':''}>${effortLabel(value)}</option>`).join('')}</select></label></div><div class="sm-noms-form-grid"><label>Tags (comma-separated)<input name="tags" value="${esc((item.tags||[]).join(', '))}" placeholder="quick, savory"></label><label><input name="favorite" type="checkbox" ${item.favorite?'checked':''}> Favorite</label></div><label>Notes<textarea name="notes">${esc(item.notes||'')}</textarea></label>${common}</form>`;
  if(kind==='pantry')return `<form class="sm-noms-form" data-form="pantry"><label>Pantry item<input name="name" required value="${esc(item.name||'')}" placeholder="Tortillas"></label><div class="sm-noms-form-grid"><label>Quantity<input name="quantity" value="${esc(item.quantity||'')}" placeholder="1"></label><label>Unit<input name="unit" value="${esc(item.unit||'')}" placeholder="pack"></label></div><label>Best by<input name="expiresAt" type="date" value="${esc(item.expiresAt||'')}"></label><label>Notes<textarea name="notes">${esc(item.notes||'')}</textarea></label>${common}</form>`;
  if(kind==='grocery')return `<form class="sm-noms-form" data-form="grocery"><label>Grocery item<input name="name" required value="${esc(item.name||'')}" placeholder="Cheese"></label><label>Quantity<input name="quantity" value="${esc(item.quantity||'')}" placeholder="1 bag"></label><label>Notes<textarea name="notes">${esc(item.notes||'')}</textarea></label>${common}</form>`;
  if(kind==='recipe')return `<form class="sm-noms-form" data-form="recipe"><label>Recipe name<input name="name" required value="${esc(item.name||'')}" placeholder="Tiny bean pasta"></label><div class="sm-noms-form-grid"><label>Effort<select name="effort"><option value="">Any effort</option>${['no-prep','easy','medium','involved'].map(value=>`<option value="${value}" ${item.effort===value?'selected':''}>${effortLabel(value)}</option>`).join('')}</select></label><label>Linked Nom<select name="nomId"><option value="">None</option>${allNoms(store.get()).map(nom=>`<option value="${esc(nom.id)}" ${item.nomId===nom.id?'selected':''}>${esc(nom.name)}</option>`).join('')}</select></label></div><label>Tags (comma-separated)<input name="tags" value="${esc((item.tags||[]).join(', '))}"></label><label>Ingredients<textarea name="ingredients">${esc(item.ingredients||'')}</textarea></label><label>Instructions<textarea name="instructions">${esc(item.instructions||'')}</textarea></label><label>Notes<textarea name="notes">${esc(item.notes||'')}</textarea></label>${common}</form>`;
  if(kind==='meal')return `<form class="sm-noms-form" data-form="meal"><div class="sm-noms-form-grid"><label>Date<input name="date" type="date" required value="${esc(item.date||dateKey())}"></label><label>Optional slot<select name="slot"><option value="">Any time</option>${['breakfast','lunch','dinner','snack','other'].map(value=>`<option ${item.slot===value?'selected':''}>${value}</option>`).join('')}</select></label></div><label>Saved Nom<select name="nomId"><option value="">Choose a Nom</option>${allNoms(store.get()).map(nom=>`<option value="${esc(nom.id)}">${esc(nom.name)}</option>`).join('')}</select></label><label>Or meal name<input name="text" value="${esc(item.text||'')}" placeholder="Something cozy"></label>${common}</form>`;
  if(kind==='today')return `<form class="sm-noms-form" data-form="today"><label>Saved Nom<select name="nomId"><option value="">Choose a Nom</option>${allNoms(store.get()).map(nom=>`<option value="${esc(nom.id)}" ${item.nomId===nom.id?'selected':''}>${esc(nom.name)}</option>`).join('')}</select></label><label>Or tell KatOS what we’re nommin<input name="text" value="${esc(item.text||'')}" placeholder="Quesadillas"></label>${common}</form>`;
  return '';
 };
 const section=(title,description,content,addLabel,kind)=>`<section class="sm-noms-section"><header><div><h2>${title}</h2><small>${description}</small></div>${kind?`<button class="primary" data-add="${kind}">${addLabel}</button>`:''}</header>${content}</section>`;
 const draw=()=>{
  const state=store.get(),foods=allNoms(state),pantry=pantryItems(state),groceries=groceryItems(state),recipes=allRecipes(state),emergency=emergencyNoms(state),today=todayNom(state),todayLabel=today?.label||'Nothing picked yet—no pressure.';
  const tiles=[['chooser','🍓 What Sounds Good?','A tiny filter or surprise pick.'],['noms','🍱 My Noms',`${foods.length} saved favorite${foods.length===1?'':'s'}.`],['pantry','🧺 Pantry',`${pantry.length} thing${pantry.length===1?'':'s'} available.`],['groceries','🛒 Groceries',`${groceries.length} item${groceries.length===1?'':'s'} to get.`],['recipes','📕 Recipes',`${recipes.length} recipe${recipes.length===1?'':'s'} remembered.`],['plan','📅 Meal Plan','A gentle date-based plan.'],['emergency','🚨 Emergency Noms',`${emergency.length} low-effort reliable option${emergency.length===1?'':'s'}.`]];
  let body='';
  if(view==='home')body=`<section class="sm-noms-fridge"><div class="sm-noms-fridge-top"><div><div class="v17-eyebrow">OPEN THE FRIDGE</div><h2>🍱 Noms</h2></div><button data-view="chooser">Pick a Nom</button></div><div class="sm-noms-tile-grid">${tiles.map(([key,title,copy])=>`<button type="button" class="sm-noms-tile" data-view="${key}"><b>${title}</b><small>${copy}</small></button>`).join('')}</div><section class="sm-noms-today"><div><small>WHAT WE NOMMIN ON TODAY? 🍓</small><h2>${esc(todayLabel)}</h2>${today?.nom?.effort?`<small>${esc(effortLabel(today.nom.effort))} • saved Nom</small>`:''}</div><div class="sm-noms-actions"><button data-set-today>Set / change</button>${today?'<button data-clear-today>Clear</button>':''}</div></section></section>`;
  if(view==='chooser'){const picks=nomsMatchingFilters(state,filters),filterButtons=[['','All'],['easy','Easy'],['no-prep','No prep'],['medium','Medium'],['involved','Involved']].map(([value,label])=>`<button data-effort="${value}" class="${filters.effort===value?'active':''}">${label}</button>`).join('');body=section('🍓 What Sounds Good?','Choose only from Noms you already saved.',`<div class="sm-noms-filter">${filterButtons}<button data-tag="sweet" class="${filters.tag==='sweet'?'active':''}">Sweet</button><button data-tag="savory" class="${filters.tag==='savory'?'active':''}">Savory</button><button data-tag="snack" class="${filters.tag==='snack'?'active':''}">Snack</button><button data-favorite class="${filters.favorite?'active':''}">Favorites</button><button data-surprise>🎲 Surprise me</button></div><div class="sm-noms-list">${picks.map(nom=>`<article class="sm-noms-row"><span><b>${esc(nom.name)}</b><small>${esc([nom.type,effortLabel(nom.effort),...(nom.tags||[])].filter(Boolean).join(' • '))}</small></span><div class="sm-noms-compact"><button data-today-nom="${esc(nom.id)}">Today</button></div></article>`).join('')||'<p class="v17-empty">No saved Noms match that little craving filter yet.</p>'}</div>`,'','');}
  if(view==='noms')body=section('🍱 My Noms','Foods, meals, favorites, and dependable options.',`${renderForm('nom',form?.item||{})}<div class="sm-noms-list">${foods.map(nom=>`<article class="sm-noms-row"><span><b>${esc(nom.name)} ${nom.favorite?'🍓':''}</b><small>${esc([nom.type,effortLabel(nom.effort),...(nom.tags||[])].filter(Boolean).join(' • '))}</small></span><div class="sm-noms-compact"><button data-edit="nom|${esc(nom.id)}">Edit</button><button data-today-nom="${esc(nom.id)}">Today</button><button data-emergency-nom="${esc(nom.id)}">Emergency</button><button data-archive="nom|${esc(nom.id)}">Archive</button></div></article>`).join('')||'<p class="v17-empty">Save a food you like so Future Kat has an easier choice.</p>'}</div>`,'+ Add a Nom','nom');
  if(view==='pantry')body=section('🧺 Pantry','A simple list of what you currently have.',`${renderForm('pantry',form?.item||{})}<div class="sm-noms-list">${pantry.map(item=>`<article class="sm-noms-row"><span><b>${esc(item.name)}</b><small>${esc([item.quantity,item.unit,item.expiresAt?`best by ${item.expiresAt}`:'',item.notes].filter(Boolean).join(' • '))}</small></span><div class="sm-noms-compact"><button data-edit="pantry|${esc(item.id)}">Edit</button><button data-archive="pantry|${esc(item.id)}">Remove</button></div></article>`).join('')||'<p class="v17-empty">The pantry is empty for now. Add only what helps.</p>'}</div>`,'+ Add pantry item','pantry');
  if(view==='groceries')body=section('🛒 Groceries','A soft little list for things to obtain.',`${renderForm('grocery',form?.item||{})}<div class="sm-noms-list">${groceries.map(item=>`<article class="sm-noms-row"><span><b>${esc(item.name)}</b><small>${esc([item.quantity,item.notes].filter(Boolean).join(' • '))}</small></span><div class="sm-noms-compact"><button data-edit="grocery|${esc(item.id)}">Edit</button><button data-obtained="${esc(item.id)}">Obtained</button><button data-pantry-from-grocery="${esc(item.id)}">Add to Pantry</button><button data-archive="grocery|${esc(item.id)}">Archive</button></div></article>`).join('')||'<p class="v17-empty">No groceries waiting right now.</p>'}</div>`,'+ Add grocery item','grocery');
  if(view==='recipes')body=section('📕 Recipes','Keep the recipes that are worth remembering.',`${renderForm('recipe',form?.item||{})}<div class="sm-noms-list">${recipes.map(recipe=>`<article class="sm-noms-row"><span><b>${esc(recipe.name)}</b><small>${esc([effortLabel(recipe.effort),...(recipe.tags||[])].filter(Boolean).join(' • '))}</small></span><div class="sm-noms-compact"><button data-edit="recipe|${esc(recipe.id)}">Edit</button><button data-archive="recipe|${esc(recipe.id)}">Archive</button></div></article>`).join('')||'<p class="v17-empty">No recipes saved yet. A simple handwritten favorite counts.</p>'}</div>`,'+ Add recipe','recipe');
  if(view==='plan'){const meals=allPlannedMeals(state);body=section('📅 Meal Plan','Assign a Nom or a freeform meal to a date.',`${renderForm('meal',form?.item||{})}<div class="sm-noms-list">${meals.map(meal=>{const nom=foods.find(item=>item.id===meal.nomId);return`<article class="sm-noms-row"><span><b>${esc(nom?.name||meal.text||'Planned meal')}</b><small>${esc([meal.date,meal.slot].filter(Boolean).join(' • '))}</small></span><div class="sm-noms-compact"><button data-today-meal="${esc(meal.id)}">Today</button><button data-archive="meal|${esc(meal.id)}">Clear</button></div></article>`}).join('')||'<p class="v17-empty">No meals planned yet. That is completely allowed.</p>'}</div>`,'+ Plan a meal','meal');}
  if(view==='emergency')body=section('🚨 Emergency Noms','Kat-picked, quick, reliable options for very low-effort moments.',`<div class="sm-noms-list">${emergency.map(nom=>`<article class="sm-noms-row"><span><b>${esc(nom.name)}</b><small>${esc([effortLabel(nom.effort),...(nom.tags||[])].filter(Boolean).join(' • '))}</small></span><div class="sm-noms-compact"><button data-today-nom="${esc(nom.id)}">Today</button></div></article>`).join('')||'<p class="v17-empty">Nothing needs to be an Emergency Nom until Kat says it is.</p>'}</div><p class="v17-muted">Add a saved Nom to this list from My Noms.</p>`,'','');
  root.innerHTML=`<section class="v17-card sm-noms"><section class="sm-noms-hero"><div class="v17-eyebrow">🍓 KATOS • FOOD &amp; KITCHEN LIFE</div><h1>🍱 Noms</h1><p>What we nommin on today?</p></section>${view!=='home'?`<p class="sm-noms-note"><button data-view="home">← Back to the fridge</button> ${esc(notice)}</p>`:''}${body}</section>`;
  root.querySelectorAll('[data-view]').forEach(button=>button.onclick=()=>{view=button.dataset.view;form=null;notice='';draw()});
  root.querySelectorAll('[data-add]').forEach(button=>button.onclick=()=>{form={kind:button.dataset.add,item:{}};notice='';draw()});root.querySelector('[data-set-today]')?.addEventListener('click',()=>{form={kind:'today',item:today||{}};draw()});root.querySelector('[data-clear-today]')?.addEventListener('click',()=>show(actions.clearTodayNom()));root.querySelectorAll('[data-cancel-form]').forEach(button=>button.onclick=()=>{form=null;draw()});
  root.querySelectorAll('[data-edit]').forEach(button=>{button.onclick=()=>{const[kind,id]=button.dataset.edit.split('|'),maps={nom:foods,pantry,grocery:groceries,recipe:recipes};form={kind,item:maps[kind]?.find(item=>item.id===id)||{}};draw()}});
  root.querySelectorAll('[data-archive]').forEach(button=>button.onclick=()=>{const[kind,id]=button.dataset.archive.split('|');const methods={nom:'archiveNom',pantry:'archivePantryItem',grocery:'archiveGroceryItem',recipe:'archiveRecipe',meal:'archivePlannedMeal'};show(actions[methods[kind]]?.(id))});
  root.querySelectorAll('[data-today-nom]').forEach(button=>button.onclick=()=>show(actions.setTodayNom({nomId:button.dataset.todayNom})));root.querySelectorAll('[data-emergency-nom]').forEach(button=>button.onclick=()=>show(actions.addEmergencyNom(button.dataset.emergencyNom)));root.querySelectorAll('[data-obtained]').forEach(button=>button.onclick=()=>show(actions.completeGroceryItem(button.dataset.obtained)));root.querySelectorAll('[data-pantry-from-grocery]').forEach(button=>button.onclick=()=>show(actions.addGroceryToPantry(button.dataset.pantryFromGrocery)));root.querySelectorAll('[data-today-meal]').forEach(button=>button.onclick=()=>{const meal=allPlannedMeals(state).find(item=>item.id===button.dataset.todayMeal);show(actions.setTodayNom({nomId:meal?.nomId,text:meal?.text}))});
  root.querySelectorAll('[data-effort]').forEach(button=>button.onclick=()=>{filters={...filters,effort:button.dataset.effort};draw()});root.querySelectorAll('[data-tag]').forEach(button=>button.onclick=()=>{filters={...filters,tag:filters.tag===button.dataset.tag?'':button.dataset.tag};draw()});root.querySelector('[data-favorite]')?.addEventListener('click',()=>{filters={...filters,favorite:!filters.favorite};draw()});root.querySelector('[data-surprise]')?.addEventListener('click',()=>{const nom=chooseSurpriseNom(state,filters);if(nom)show(actions.setTodayNom({nomId:nom.id}));else{notice='No saved Nom matches that filter yet.';draw()}});
  root.querySelectorAll('.sm-noms-form').forEach(node=>node.addEventListener('submit',event=>{event.preventDefault();const data=Object.fromEntries(new FormData(event.currentTarget)),kind=event.currentTarget.dataset.form,existing=form?.item,method=existing?.id?{nom:'updateNom',pantry:'updatePantryItem',grocery:'updateGroceryItem',recipe:'updateRecipe'}[kind]:{nom:'addNom',pantry:'addPantryItem',grocery:'addGroceryItem',recipe:'addRecipe',meal:'setPlannedMeal',today:'setTodayNom'}[kind];const saved=['nom','pantry','grocery','recipe'].includes(kind)&&existing?.id?actions[method](existing.id,data):actions[method](data);form=null;show(saved)}));
 };
 store.subscribe(draw);draw();
}
export{renderNoms};
