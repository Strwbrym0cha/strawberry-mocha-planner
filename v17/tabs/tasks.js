const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const id=()=>Date.now().toString(36)+Math.random().toString(36).slice(2,6);
function renderTasks({root,store}){const draw=()=>{const d=store.get(),day=new Date().toISOString().slice(0,10),tasks=d.tasks||[],todayTasks=tasks.filter(t=>!t.date||t.date===day),routines=d.routines||[];root.innerHTML=`<section class="v17-card"><div class="v17-home-hero"><div class="v17-eyebrow">🍓 STRAWBERRY MOCHA • TASKS</div><h1>📝 Tasks & Routines</h1><p>Break today into tiny, doable pieces. ♡</p></div><div class="v17-card"><header><h2>☑️ Today's Tasks</h2><span class="v17-pill">${todayTasks.filter(t=>t.done).length}/${todayTasks.length}</span></header><div class="v17-progress"><i style="width:${todayTasks.length?Math.round(todayTasks.filter(t=>t.done).length/todayTasks.length*100):0}%"></i></div>${todayTasks.map(t=>`<div class="v17-task-row"><label class="v17-check"><input type="checkbox" data-task="${esc(t.id)}" ${t.done?'checked':''}><span>${esc(t.text||t.title||'Untitled task')}</span></label><button type="button" class="v17-delete-task" data-delete-task="${esc(t.id)}" aria-label="Delete task">×</button></div>`).join('')||'<div class="v17-empty">✨ No tasks yet. Add one below.</div>'}<div class="v17-add-event" style="margin-top:10px"><input id="newTask" placeholder="Add a task…"><input id="taskDate" type="date" value="${day}"><button id="addTask" type="button">＋ Add</button></div></div><div class="v17-card"><header><h2>🎀 Routines</h2><span class="v17-muted">Your routine steps will grow here.</span></header>${routines.map(r=>{const steps=Array.isArray(r.steps)?r.steps:[],checks=r.checks?.[day]||{},done=steps.filter((_,i)=>checks[i]).length;return `<article class="v17-mini"><span><b>${esc(r.name||'Routine')}</b><small style="display:block;color:var(--muted)">${steps.length?done+'/'+steps.length+' steps':'No steps added yet'}</small></span><span>${steps.length?Math.round(done/steps.length*100)+'%':'—'}</span></article>`}).join('')||'<div class="v17-empty">🌷 No routines yet.</div>'}</div></section>`;root.querySelectorAll('[data-task]').forEach(c=>c.onchange=()=>store.update(x=>({...x,tasks:(x.tasks||[]).map(t=>String(t.id)===String(c.dataset.task)?{...t,done:c.checked}:t)})));root.querySelectorAll('[data-delete-task]').forEach(b=>b.onclick=()=>store.update(x=>({...x,tasks:(x.tasks||[]).filter(t=>String(t.id)!==String(b.dataset.deleteTask))})));root.querySelector('#addTask').onclick=()=>{const input=root.querySelector('#newTask'),text=input.value.trim();if(!text)return;const date=root.querySelector('#taskDate').value||day;store.update(x=>({...x,tasks:[...(x.tasks||[]),{id:id(),text,date,done:false}]}))};};store.subscribe(draw);draw()}
export{renderTasks};