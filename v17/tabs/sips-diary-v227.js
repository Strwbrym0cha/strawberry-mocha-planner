const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
const list=value=>Array.isArray(value)?value:[];
const todayKey=()=>{const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`};
const makeId=()=>`sip-${Date.now().toString(36)}-${Math.random().toString(36).slice(2,8)}`;
const states=new WeakMap();

function style(){if(document.getElementById('sm-sip-diary-style'))return;const el=document.createElement('style');el.id='sm-sip-diary-style';el.textContent=`
.sm-sip-quick,.sm-sip-diary{margin-top:12px;padding:20px;border:1px solid #d6e5e9;border-radius:24px;background:linear-gradient(135deg,#f2fbff,#fffdfb 58%,#f2faee);box-shadow:0 10px 28px rgba(77,112,128,.06)}
.sm-sip-quick-head,.sm-sip-diary-head{display:flex;justify-content:space-between;align-items:flex-start;gap:12px;margin-bottom:12px}.sm-sip-quick h2,.sm-sip-diary h2{margin:3px 0}.sm-sip-quick small,.sm-sip-diary small{color:#7f8183}.sm-sip-log-form{display:grid;grid-template-columns:1.4fr .7fr auto;gap:9px}.sm-sip-log-form label{display:grid;gap:5px;font-size:11px;font-weight:900;color:#727d81}.sm-sip-log-form input{min-height:44px;padding:10px 11px;border:1px solid #cfe0e6;border-radius:14px;background:#fff;color:#674941;font:inherit}.sm-sip-log-form button{align-self:end;min-height:44px;border-radius:14px}.sm-sip-recents{display:flex;gap:7px;flex-wrap:wrap;margin-top:10px}.sm-sip-recents button{padding:7px 10px;border-radius:999px;font-size:11px}
.sm-sip-diary-date{display:flex;align-items:center;gap:7px;flex-wrap:wrap}.sm-sip-diary-date input{min-height:40px;padding:8px 10px;border:1px solid #cfe0e6;border-radius:13px;background:#fff;color:#674941}.sm-sip-breakdown{display:flex;gap:8px;flex-wrap:wrap;margin:10px 0 13px}.sm-sip-chip{padding:8px 11px;border-radius:999px;background:#eef8fb;border:1px solid #cfe3ea;color:#54727d;font-size:12px;font-weight:900}.sm-sip-total{background:#fff0f6;border-color:#efcad8;color:#a35f79}.sm-sip-diary-list{display:grid;gap:8px}.sm-sip-diary-row{display:grid;grid-template-columns:72px minmax(0,1fr) auto auto;gap:10px;align-items:center;padding:11px 12px;border:1px solid #e2e9e8;border-radius:16px;background:rgba(255,255,255,.86)}.sm-sip-diary-row time{font-size:11px;font-weight:900;color:#658697}.sm-sip-diary-row b{display:block}.sm-sip-diary-row strong{white-space:nowrap}.sm-sip-diary-row button{width:32px;height:32px;padding:0;border:1px solid #d8e4e7;border-radius:50%;background:#fff;color:#7a7f82}.sm-sip-diary-empty{padding:16px;border:1px dashed #cfe0e6;border-radius:16px;text-align:center;color:#7f8183;background:#fbfeff}
@media(max-width:700px){.sm-sip-log-form{grid-template-columns:1fr 1fr}.sm-sip-log-form button{grid-column:1/-1}.sm-sip-diary-head{flex-direction:column}.sm-sip-diary-date{width:100%}.sm-sip-diary-date input{flex:1}.sm-sip-diary-row{grid-template-columns:58px minmax(0,1fr) auto auto}}
`;document.head.appendChild(el)}

function entries(state){return list(state?.sips?.entries)}
function previousDrinks(state){const names=[String(state?.sips?.drink||'Water'),...entries(state).slice().reverse().map(entry=>String(entry?.drink||'').trim())].filter(Boolean);return [...new Set(names.map(name=>name.toLowerCase()))].map(key=>names.find(name=>name.toLowerCase()===key)).slice(0,6)}
function breakdown(dayEntries){const map=new Map();dayEntries.forEach(entry=>{const name=String(entry?.drink||'Drink').trim()||'Drink',key=name.toLowerCase(),amount=Number(entry?.amountOz)||0,current=map.get(key)||{name,total:0};current.total+=amount;map.set(key,current)});return [...map.values()].sort((a,b)=>b.total-a.total)}

function logSip(store,drink,amount){drink=String(drink||'').trim();amount=Number(amount);if(!drink||!Number.isFinite(amount)||amount<=0)return false;const now=new Date().toISOString(),entry={id:makeId(),date:todayKey(),drink,amountOz:Math.min(512,amount),createdAt:now};store.update(state=>({...state,sips:{...(state.sips||{}),entries:[...entries(state),entry]}}));return true}

function patchBase(root){const sips=root.querySelector('.sm-sips');if(!sips)return;const firstCard=sips.querySelector('.sm-sips-grid .sm-sips-card:first-child');const title=firstCard?.querySelector('h2');if(title)title.textContent="Today's fluids";const setup=sips.querySelector('.sm-sips-settings');const firstLabel=setup?.querySelector('label');if(firstLabel&&firstLabel.firstChild?.nodeType===3)firstLabel.firstChild.textContent='Default quick-log drink';}

function render(root,store){
 const host=root.querySelector('.sm-sips');if(!host)return;patchBase(root);const model=states.get(root)||{date:todayKey()};states.set(root,model);const state=store.get?.()||{};
 let quick=root.querySelector('#sm-sip-quick');if(!quick){quick=document.createElement('section');quick.id='sm-sip-quick';quick.className='sm-sip-quick';const hero=host.querySelector('.sm-sips-hero');hero?.insertAdjacentElement('afterend',quick)}
 const defaultDrink=String(state?.sips?.drink||'Water'),defaultAmount=Number(state?.sips?.servingOz)||8,recents=previousDrinks(state);
 quick.innerHTML=`<div class="sm-sip-quick-head"><div><div class="v17-eyebrow">🫧 QUICK LOG</div><h2>Log a Sip</h2><small>Each drink gets its own name and ounces. Your default drink is only a shortcut.</small></div></div><div class="sm-sip-log-form"><label>Drink<input data-sip-log-drink value="${esc(defaultDrink)}" list="sm-sip-drinks"></label><label>Ounces<input data-sip-log-amount type="number" min="0.5" step="0.5" value="${defaultAmount}"></label><button type="button" class="primary" data-sip-log>＋ Log Sip</button></div><datalist id="sm-sip-drinks">${recents.map(name=>`<option value="${esc(name)}"></option>`).join('')}</datalist><div class="sm-sip-recents">${recents.map(name=>`<button type="button" data-sip-pick="${esc(name)}">${esc(name)}</button>`).join('')}</div>`;
 quick.querySelector('[data-sip-log]')?.addEventListener('click',()=>{const drink=quick.querySelector('[data-sip-log-drink]')?.value,amount=quick.querySelector('[data-sip-log-amount]')?.value;if(!logSip(store,drink,amount))alert('Give the drink a name and an amount greater than 0 oz.')});
 quick.querySelectorAll('[data-sip-pick]').forEach(button=>button.addEventListener('click',()=>{const input=quick.querySelector('[data-sip-log-drink]');if(input)input.value=button.dataset.sipPick||''}));

 let diary=root.querySelector('#sm-sip-diary');if(!diary){diary=document.createElement('section');diary.id='sm-sip-diary';diary.className='sm-sip-diary';host.appendChild(diary)}
 const dayEntries=entries(state).filter(entry=>String(entry?.date||'')===String(model.date)).slice().sort((a,b)=>String(a.createdAt||'').localeCompare(String(b.createdAt||''))),groups=breakdown(dayEntries),total=dayEntries.reduce((sum,entry)=>sum+(Number(entry?.amountOz)||0),0),dateLabel=model.date===todayKey()?'Today':new Date(`${model.date}T12:00:00`).toLocaleDateString([],{weekday:'long',month:'short',day:'numeric'});
 diary.innerHTML=`<div class="sm-sip-diary-head"><div><div class="v17-eyebrow">💧 SIP DIARY</div><h2>${esc(dateLabel)}'s drinks</h2><small>See exactly what made up the day instead of one mystery hydration number.</small></div><div class="sm-sip-diary-date"><input type="date" data-sip-diary-date value="${esc(model.date)}"><button type="button" data-sip-diary-today>Today</button></div></div><div class="sm-sip-breakdown"><span class="sm-sip-chip sm-sip-total">Total ${total} oz</span>${groups.map(group=>`<span class="sm-sip-chip">${esc(group.name)} ${group.total} oz</span>`).join('')}</div><div class="sm-sip-diary-list">${[...dayEntries].reverse().map(entry=>{const time=entry.createdAt?new Date(entry.createdAt).toLocaleTimeString([],{hour:'numeric',minute:'2-digit'}):'';return `<article class="sm-sip-diary-row"><time>${esc(time)}</time><span><b>${esc(entry.drink||'Drink')}</b></span><strong>${Number(entry.amountOz)||0} oz</strong><button type="button" data-sip-delete="${esc(entry.id)}" aria-label="Delete sip entry">×</button></article>`}).join('')||'<div class="sm-sip-diary-empty">No drinks logged for this day yet. 💧</div>'}</div>`;
 diary.querySelector('[data-sip-diary-date]')?.addEventListener('change',event=>{model.date=event.target.value||todayKey();render(root,store)});
 diary.querySelector('[data-sip-diary-today]')?.addEventListener('click',()=>{model.date=todayKey();render(root,store)});
 diary.querySelectorAll('[data-sip-delete]').forEach(button=>button.addEventListener('click',()=>{const id=String(button.dataset.sipDelete);store.update(s=>({...s,sips:{...(s.sips||{}),entries:entries(s).filter(entry=>String(entry?.id)!==id)}}))}));
}

export function installSipDiary({root,store}){style();let queued=false;const scan=()=>{if(queued)return;queued=true;requestAnimationFrame(()=>{queued=false;render(root,store)})};new MutationObserver(scan).observe(root,{childList:true,subtree:true});store.subscribe?.(scan);scan()}
