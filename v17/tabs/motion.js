const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const list=v=>Array.isArray(v)?v:[];
const id=p=>`${p}-${Date.now().toString(36)}-${Math.random().toString(36).slice(2,8)}`;
const dateKey=(d=new Date())=>`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;
const today=()=>dateKey();
const nowTime=()=>new Date().toTimeString().slice(0,5);
const num=(v,f=0)=>Number.isFinite(Number(v))?Number(v):f;
const safeUrl=v=>{try{const u=new URL(String(v||'').trim());return /^https?:$/.test(u.protocol)?u.href:''}catch{return''}};
const startOfWeek=(value=new Date())=>{const d=new Date(value);const day=d.getDay();const diff=(day+6)%7;d.setHours(12,0,0,0);d.setDate(d.getDate()-diff);return d};
const weekDays=()=>Array.from({length:7},(_,i)=>{const d=startOfWeek();d.setDate(d.getDate()+i);return d});
const sundayKey=()=>dateKey(weekDays()[6]);
const prettyDate=v=>{if(!v)return'';const d=new Date(`${v}T12:00:00`);return Number.isNaN(d.getTime())?v:d.toLocaleDateString([],{weekday:'short',month:'short',day:'numeric'})};
const motionState=state=>({sessions:list(state?.motion?.sessions),routines:list(state?.motion?.routines),videos:list(state?.motion?.videos),plans:list(state?.motion?.plans),weighIns:list(state?.motion?.weighIns)});
const typeIcon=t=>({Pilates:'🧘🏽‍♀️',Treadmill:'🏃🏽‍♀️',Walk:'🚶🏽‍♀️',Stretch:'🌷',Strength:'💪🏽',Dance:'💃🏽',Other:'✨'}[t]||'✨');

function installStyle(){if(document.getElementById('sm-motion-style'))return;const s=document.createElement('style');s.id='sm-motion-style';s.textContent=`
body.sm-motion-modal-open{overflow:hidden!important;overscroll-behavior:none!important}
.sm-motion{color:#674941}.sm-motion-hero{padding:26px;border:1.5px solid #d9e6ce;border-radius:30px;background:radial-gradient(circle at 86% 24%,rgba(244,192,215,.42),transparent 24%),linear-gradient(135deg,#f1faed,#fffdf9 54%,#fff0f6);position:relative;overflow:hidden}.sm-motion-hero:after{content:'🌷  🧘🏽‍♀️  ✦';position:absolute;right:22px;bottom:14px;font-size:25px;opacity:.72}.sm-motion-hero h1{margin:4px 0}.sm-motion-hero p{max-width:650px;margin-bottom:0;color:#876f69}
.sm-motion-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:11px;margin-top:14px}.sm-motion-stat{padding:16px;border:1px solid #dfdfd4;border-radius:21px;background:rgba(255,255,255,.86);box-shadow:0 8px 22px rgba(103,73,65,.06)}.sm-motion-stat small,.sm-motion-card small{display:block;color:#927c76}.sm-motion-stat strong{display:block;font-size:22px;margin-top:5px;color:#665048}.sm-motion-card{margin-top:13px;padding:20px;border:1px solid #e3ddd5;border-radius:25px;background:linear-gradient(140deg,#fffdfb,#fff9fb 62%,#f6fbf1);box-shadow:0 10px 28px rgba(103,73,65,.055)}.sm-motion-card>header{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;margin-bottom:13px}.sm-motion-card h2,.sm-motion-card h3{margin:3px 0}.sm-motion-actions{display:flex;gap:8px;flex-wrap:wrap}.sm-motion-actions button,.sm-motion-actions a{min-height:38px;padding:8px 11px;border:1px solid #e6d8d8;border-radius:13px;background:#fff;color:#745b55;font:inherit;font-weight:800;text-decoration:none;display:inline-grid;place-items:center}.sm-motion-actions .primary{border:0;background:linear-gradient(105deg,#da96b4,#b9d5a5);color:#fff}.sm-motion-today{border:2px dashed #d7b7c5;background:linear-gradient(105deg,#fff0f6,#f3f9ee)}.sm-motion-plan{padding:14px;border:1px solid #eadce0;border-radius:18px;background:rgba(255,255,255,.83);display:grid;grid-template-columns:minmax(0,1fr) auto;gap:12px;align-items:center;margin-top:9px}.sm-motion-plan b,.sm-motion-plan small{display:block}.sm-motion-plan.done{opacity:.72}.sm-motion-plan.skipped{opacity:.62}.sm-motion-badge{display:inline-flex;align-items:center;gap:5px;padding:5px 9px;border-radius:999px;background:#f5edf1;color:#a2607c;font-size:10px;font-weight:950;letter-spacing:.04em;text-transform:uppercase}.sm-motion-week{display:grid;grid-template-columns:repeat(7,minmax(0,1fr));gap:7px}.sm-motion-day{min-height:110px;padding:10px;border:1px solid #e7ded8;border-radius:16px;background:#fff}.sm-motion-day.today{border-color:#d98dac;box-shadow:0 0 0 3px rgba(217,141,172,.11)}.sm-motion-day strong,.sm-motion-day small{display:block}.sm-motion-day .dots{margin-top:7px;display:flex;gap:4px;flex-wrap:wrap}.sm-motion-dot{font-size:13px}.sm-motion-list{display:grid;gap:9px}.sm-motion-row{padding:14px;border:1px solid #eadfe1;border-radius:18px;background:#fff;display:grid;grid-template-columns:minmax(0,1fr) auto;gap:12px;align-items:center}.sm-motion-row b,.sm-motion-row small{display:block}.sm-motion-row small{margin-top:4px;line-height:1.35}.sm-motion-steps{margin:8px 0 0;padding-left:19px;color:#806c66;font-size:12px}.sm-motion-steps li{margin:3px 0}.sm-motion-video-link{overflow-wrap:anywhere}.sm-motion-faves{display:flex;gap:8px;flex-wrap:wrap}.sm-motion-fave{padding:9px 12px;border-radius:999px;background:#f5faef;border:1px solid #d8e7cd;color:#66805d;font-weight:850}.sm-motion-empty{padding:17px;border:1px dashed #dccdce;border-radius:17px;background:#fffafd;text-align:center;color:#927c76}.sm-motion-weight{display:grid;grid-template-columns:1fr 1fr;gap:11px}.sm-motion-weight-box{padding:16px;border-radius:19px;border:1px solid #e6dad6;background:#fff}.sm-motion-weight-box strong{display:block;font-size:25px;margin-top:4px}.sm-motion-due{background:#fff1f6;border-color:#ecc5d4}.sm-motion-modal{position:fixed;inset:0;z-index:2100;background:rgba(75,53,62,.36);backdrop-filter:blur(12px);-webkit-backdrop-filter:blur(12px);padding:18px;display:flex;align-items:flex-start;justify-content:center;overflow:hidden;touch-action:none}.sm-motion-sheet{width:min(720px,100%);max-height:calc(100dvh - 36px);overflow-y:auto;overflow-x:hidden;-webkit-overflow-scrolling:touch;overscroll-behavior:contain;touch-action:pan-y;margin:auto;padding:26px;border:2px solid #f0d5df;border-radius:30px;background:linear-gradient(140deg,#fff3f8,#fffdf9 58%,#f1f9ec);box-shadow:0 28px 80px rgba(67,43,55,.28)}.sm-motion-sheet>header{display:flex;justify-content:space-between;gap:12px;align-items:flex-start;padding-bottom:15px;margin-bottom:16px;border-bottom:2px dashed #e8c7d3}.sm-motion-sheet h2{margin:3px 0}.sm-motion-close{width:44px;height:44px;border-radius:15px;border:1px solid #e9c8d4;background:#fff;color:#b86584;font-size:24px}.sm-motion-form{display:grid;gap:11px}.sm-motion-form-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.sm-motion-form label{display:grid;gap:6px;font-size:11px;font-weight:950;color:#9b687a}.sm-motion-form input,.sm-motion-form select,.sm-motion-form textarea{width:100%;min-height:45px;padding:10px 12px;border:1.5px solid #e7cbd5;border-radius:14px;background:#fff;color:#674941;font:inherit}.sm-motion-form textarea{min-height:90px;resize:vertical}.sm-motion-check{display:flex!important;grid-template-columns:auto 1fr!important;align-items:center;gap:8px!important;padding:11px;border:1px solid #ead9df;border-radius:15px;background:#fff}.sm-motion-check input{width:18px!important;min-height:18px!important}.sm-motion-footer{position:sticky;bottom:-1px;display:flex;justify-content:flex-end;gap:8px;margin-top:8px;padding:17px 0 2px;background:linear-gradient(180deg,rgba(255,253,249,0),#fffdf9 35%)}.sm-motion-footer button{min-width:145px}.sm-motion-run-step{display:flex;gap:10px;align-items:flex-start;padding:12px;border:1px solid #e6ddda;border-radius:16px;background:#fff;margin:8px 0}.sm-motion-run-step input{width:20px;height:20px}.sm-motion-progress{height:12px;background:#edf0e9;border-radius:999px;overflow:hidden;margin:12px 0}.sm-motion-progress i{display:block;height:100%;background:linear-gradient(90deg,#d98fac,#b7d4a4);border-radius:999px}
@media(max-width:900px){.sm-motion-grid{grid-template-columns:repeat(2,1fr)}.sm-motion-week{grid-template-columns:repeat(4,1fr)}}@media(max-width:620px){.sm-motion-grid,.sm-motion-form-grid,.sm-motion-weight{grid-template-columns:1fr}.sm-motion-week{grid-template-columns:repeat(2,1fr)}.sm-motion-plan,.sm-motion-row{grid-template-columns:1fr}.sm-motion-sheet{max-height:calc(100dvh - 20px);padding:20px;border-radius:25px}.sm-motion-modal{padding:10px}.sm-motion-footer{flex-direction:column}.sm-motion-footer button{width:100%}}
`;document.head.appendChild(s)}

function renderMotion({root,store}){
 installStyle();let modal=null,editingId=null,runChecks=[];
 const saveMotion=patch=>store.update(state=>({...state,motion:{...motionState(state),...(state.motion||{}),...patch}}));
 const get=()=>motionState(store.get());
 const closeModal=()=>{modal=null;editingId=null;runChecks=[];draw()};
 const open=(kind,idValue=null)=>{modal=kind;editingId=idValue;runChecks=[];draw()};
 const linkedSource=(m,kind,sourceId)=>kind==='routine'?m.routines.find(x=>String(x.id)===String(sourceId)):kind==='video'?m.videos.find(x=>String(x.id)===String(sourceId)):null;
 const planLabel=(m,p)=>p.label||linkedSource(m,p.sourceType,p.sourceId)?.name||linkedSource(m,p.sourceType,p.sourceId)?.title||'Movement';
 const addPlan=(sourceType,sourceId,label,date=today())=>{const m=get();const existing=m.plans.find(p=>p.date===date&&p.sourceType===sourceType&&String(p.sourceId||'')===String(sourceId||'')&&p.status!=='skipped');if(existing)return;saveMotion({plans:[...m.plans,{id:id('moveplan'),date,sourceType,sourceId:sourceId||null,label:label||'',status:'planned',createdAt:new Date().toISOString()}]})};
 const addToTasks=plan=>{const state=store.get(),tasks=list(state.tasks);if(tasks.some(t=>String(t.sourceMotionPlanId||'')===String(plan.id)))return alert('This movement plan is already in Sweet To-Dos. 🌷');const m=get(),source=linkedSource(m,plan.sourceType,plan.sourceId),duration=num(source?.durationMin,0),task={id:id('task-motion'),text:`🌷 ${planLabel(m,plan)}`,date:plan.date,done:false,category:'Movement',effort:'Medium',durationMin:duration,source:'motion',sourceMotionPlanId:plan.id};store.update(s=>({...s,tasks:[...list(s.tasks),task]}))};
 const updatePlan=(pid,changes)=>{const m=get();saveMotion({plans:m.plans.map(p=>String(p.id)===String(pid)?{...p,...changes,updatedAt:new Date().toISOString()}:p)})};
 const deleteItem=(key,itemId)=>{const m=get();saveMotion({[key]:m[key].filter(x=>String(x.id)!==String(itemId))})};
 const sessionFromSource=(kind,source)=>({type:source?.type||'Pilates',title:source?.name||source?.title||'',durationMin:source?.durationMin||'',sourceType:kind,sourceId:source?.id||null});

 const modalMarkup=(m)=>{
  if(!modal)return'';
  let body='',title='';
  if(modal==='routine'){
   const x=editingId?m.routines.find(r=>String(r.id)===String(editingId))||{}:{};title=editingId?'Edit Movement Routine':'New Movement Routine';body=`<form class="sm-motion-form" data-form="routine"><div class="sm-motion-form-grid"><label>Name<input name="name" required value="${esc(x.name||'')}" placeholder="Gentle Pilates"></label><label>Type<select name="type">${['Pilates','Treadmill','Walk','Stretch','Strength','Dance','Other'].map(v=>`<option ${x.type===v?'selected':''}>${v}</option>`).join('')}</select></label></div><div class="sm-motion-form-grid"><label>Estimated minutes<input name="durationMin" type="number" min="0" step="5" value="${esc(x.durationMin||'')}"></label><label>Difficulty<select name="difficulty"><option value="">No label</option>${['Gentle','Easy','Medium','Spicy'].map(v=>`<option ${x.difficulty===v?'selected':''}>${v}</option>`).join('')}</select></label></div><label>Routine steps, one per line<textarea name="steps" placeholder="Breathing\nHundreds\nLeg circles\nBridges\nStretch">${esc(list(x.steps).join('\n'))}</textarea></label><label>Notes<textarea name="notes" placeholder="Anything useful to remember">${esc(x.notes||'')}</textarea></label><div class="sm-motion-form-grid"><label class="sm-motion-check"><input name="favorite" type="checkbox" ${x.favorite?'checked':''}><span>⭐ Favorite</span></label><label class="sm-motion-check"><input name="emergency" type="checkbox" ${x.emergency?'checked':''}><span>🌱 Low-energy / emergency pick</span></label></div><div class="sm-motion-footer"><button type="button" data-close>Cancel</button><button class="primary" type="submit">♡ Save routine</button></div></form>`;
  } else if(modal==='video'){
   const x=editingId?m.videos.find(v=>String(v.id)===String(editingId))||{}:{};title=editingId?'Edit Video':'Save a Movement Video';body=`<form class="sm-motion-form" data-form="video"><label>Video title<input name="title" required value="${esc(x.title||'')}" placeholder="20 min beginner Pilates"></label><label>Link<input name="url" type="url" required value="${esc(x.url||'')}" placeholder="https://..."></label><div class="sm-motion-form-grid"><label>Creator<input name="creator" value="${esc(x.creator||'')}"></label><label>Type<select name="type">${['Pilates','Treadmill','Walk','Stretch','Strength','Dance','Other'].map(v=>`<option ${x.type===v?'selected':''}>${v}</option>`).join('')}</select></label></div><div class="sm-motion-form-grid"><label>Minutes<input name="durationMin" type="number" min="0" step="1" value="${esc(x.durationMin||'')}"></label><label>Difficulty<select name="difficulty"><option value="">No label</option>${['Gentle','Easy','Medium','Spicy'].map(v=>`<option ${x.difficulty===v?'selected':''}>${v}</option>`).join('')}</select></label></div><label>Status<select name="status">${['Want to Try','Done Before'].map(v=>`<option ${x.status===v?'selected':''}>${v}</option>`).join('')}</select></label><label class="sm-motion-check"><input name="favorite" type="checkbox" ${x.favorite?'checked':''}><span>⭐ Favorite video</span></label><label>Notes<textarea name="notes">${esc(x.notes||'')}</textarea></label><div class="sm-motion-footer"><button type="button" data-close>Cancel</button><button class="primary" type="submit">♡ Save video</button></div></form>`;
  } else if(modal==='session'){
   let x=editingId?m.sessions.find(s=>String(s.id)===String(editingId))||{}:{};if(!editingId&&window.__smMotionPrefill){x={...window.__smMotionPrefill};window.__smMotionPrefill=null}title=editingId?'Edit Movement Log':'Log Movement';body=`<form class="sm-motion-form" data-form="session"><div class="sm-motion-form-grid"><label>Date<input name="date" type="date" value="${esc(x.date||today())}"></label><label>Time<input name="time" type="time" value="${esc(x.time||nowTime())}"></label></div><div class="sm-motion-form-grid"><label>Movement type<select name="type">${['Pilates','Treadmill','Walk','Stretch','Strength','Dance','Other'].map(v=>`<option ${x.type===v?'selected':''}>${v}</option>`).join('')}</select></label><label>Title<input name="title" value="${esc(x.title||'')}" placeholder="Treadmill walk"></label></div><div class="sm-motion-form-grid"><label>Minutes<input name="durationMin" type="number" min="0" step="1" value="${esc(x.durationMin||'')}"></label><label>Distance, miles<input name="distanceMi" type="number" min="0" step="0.01" value="${esc(x.distanceMi||'')}"></label></div><div class="sm-motion-form-grid"><label>Treadmill speed<input name="speed" type="number" min="0" step="0.1" value="${esc(x.speed||'')}"></label><label>Incline<input name="incline" type="number" min="0" step="0.5" value="${esc(x.incline||'')}"></label></div><label>How did it feel?<select name="feel"><option value="">No rating</option>${['🌱 Gentle','🙂 Good','🔥 Challenging','🥵 A lot','✨ Amazing'].map(v=>`<option ${x.feel===v?'selected':''}>${v}</option>`).join('')}</select></label><label>Body notes<textarea name="bodyNotes" placeholder="Ankle felt stiff, energy was low, this felt really good...">${esc(x.bodyNotes||'')}</textarea></label><label>Other notes<textarea name="notes">${esc(x.notes||'')}</textarea></label><input type="hidden" name="sourceType" value="${esc(x.sourceType||'')}"><input type="hidden" name="sourceId" value="${esc(x.sourceId||'')}"><input type="hidden" name="planId" value="${esc(x.planId||'')}"><div class="sm-motion-footer"><button type="button" data-close>Cancel</button><button class="primary" type="submit">♡ Save movement</button></div></form>`;
  } else if(modal==='plan'){
   title='Plan Movement';body=`<form class="sm-motion-form" data-form="plan"><label>Date<input name="date" type="date" value="${today()}"></label><label>Movement name<input name="label" required placeholder="Pilates, treadmill walk, stretch..."></label><label class="sm-motion-check"><input name="task" type="checkbox"><span>Also add this plan to Sweet To-Dos</span></label><div class="sm-motion-footer"><button type="button" data-close>Cancel</button><button class="primary" type="submit">♡ Save plan</button></div></form>`;
  } else if(modal==='weight'){
   title='Sunday Check-In';const x=editingId?m.weighIns.find(w=>String(w.id)===String(editingId))||{}:{};body=`<form class="sm-motion-form" data-form="weight"><label>Week / date<input name="date" type="date" value="${esc(x.date||sundayKey())}"></label><label>Weight, lb<input name="weightLb" type="number" min="1" step="0.1" required value="${esc(x.weightLb||'')}"></label><label>Optional note<textarea name="note" placeholder="Anything you want to remember about this week">${esc(x.note||'')}</textarea></label><small>Sunday is the default weigh-in day, but catching up later still counts. 🌷</small><div class="sm-motion-footer"><button type="button" data-close>Cancel</button><button class="primary" type="submit">♡ Save check-in</button></div></form>`;
  } else if(modal==='runner'){
   const r=m.routines.find(x=>String(x.id)===String(editingId));if(!r){setTimeout(closeModal,0);return''}title=`${typeIcon(r.type)} ${r.name}`;const steps=list(r.steps);if(runChecks.length!==steps.length)runChecks=steps.map(()=>false);const done=runChecks.filter(Boolean).length;body=`<div><small>MOVE MODE</small><h3>${done}/${steps.length} steps complete</h3><div class="sm-motion-progress"><i style="width:${steps.length?Math.round(done/steps.length*100):0}%"></i></div>${steps.length?steps.map((step,i)=>`<label class="sm-motion-run-step"><input type="checkbox" data-run-step="${i}" ${runChecks[i]?'checked':''}><span>${esc(step)}</span></label>`).join(''):'<div class="sm-motion-empty">This routine has no steps yet. You can still log it as a session.</div>'}<div class="sm-motion-footer"><button type="button" data-close>Close</button><button class="primary" type="button" data-finish-run>✓ Finish & log</button></div></div>`;
  }
  return `<div class="sm-motion-modal" data-motion-modal><section class="sm-motion-sheet"><header><div><div class="v17-eyebrow">🌷 MOTION MEADOW</div><h2>${esc(title)}</h2></div><button type="button" class="sm-motion-close" data-close>×</button></header>${body}</section></div>`;
 };

 const draw=()=>{
  const m=get(),day=today(),days=weekDays(),weekKeys=days.map(dateKey),weekSessions=m.sessions.filter(s=>weekKeys.includes(s.date)),weekMinutes=weekSessions.reduce((sum,s)=>sum+num(s.durationMin),0),pilates=weekSessions.filter(s=>s.type==='Pilates').length,treadmillMiles=weekSessions.filter(s=>s.type==='Treadmill').reduce((sum,s)=>sum+num(s.distanceMi),0),todayPlans=m.plans.filter(p=>p.date===day),latestWeights=[...m.weighIns].sort((a,b)=>String(b.date).localeCompare(String(a.date))),latest=latestWeights[0],previous=latestWeights[1],weightDelta=latest&&previous?Math.round((num(latest.weightLb)-num(previous.weightLb))*10)/10:null,isSunday=new Date().getDay()===0,sundayEntry=m.weighIns.find(w=>w.date===sundayKey()),favorites=m.routines.filter(r=>r.favorite),emergency=m.routines.filter(r=>r.emergency);
  document.body.classList.toggle('sm-motion-modal-open',!!modal);
  const weekMarkup=days.map(d=>{const key=dateKey(d),sessions=m.sessions.filter(s=>s.date===key),plans=m.plans.filter(p=>p.date===key),weight=m.weighIns.find(w=>w.date===key),todayClass=key===day?' today':'';const icons=[...sessions.map(s=>typeIcon(s.type)),...plans.filter(p=>p.status==='skipped').map(()=> '💤')];return `<div class="sm-motion-day${todayClass}"><small>${d.toLocaleDateString([],{weekday:'short'}).toUpperCase()}</small><strong>${d.getDate()}</strong><div class="dots">${icons.map(x=>`<span class="sm-motion-dot">${x}</span>`).join('')||'<span class="sm-motion-dot">·</span>'}</div>${sessions.length?`<small>${sessions.reduce((a,s)=>a+num(s.durationMin),0)} min</small>`:''}${weight?`<small>⚖️ ${esc(weight.weightLb)} lb</small>`:''}</div>`}).join('');
  const planRows=todayPlans.map(p=>{const status=p.status||'planned',cls=status==='complete'?' done':status==='skipped'?' skipped':'',label=planLabel(m,p);return `<article class="sm-motion-plan${cls}"><div><span class="sm-motion-badge">${status==='complete'?'✓ complete':status==='skipped'?'💤 skipped':'today'}</span><b>${esc(label)}</b><small>${p.sourceType==='routine'?'Movement routine':p.sourceType==='video'?'Saved video':'Flexible plan'}</small></div><div class="sm-motion-actions">${status==='planned'?`<button class="primary" data-plan-log="${esc(p.id)}">✓ Complete & log</button><button data-plan-skip="${esc(p.id)}">Skip Today</button><button data-plan-task="${esc(p.id)}">＋ Sweet To-Dos</button>`:`<button data-plan-reset="${esc(p.id)}">Reset</button>`}<button data-delete-plan="${esc(p.id)}">×</button></div></article>`}).join('')||'<div class="sm-motion-empty">Nothing planned today. Spontaneous movement counts too. 🌱</div>';
  const routineRows=m.routines.map(r=>`<article class="sm-motion-row"><div><b>${typeIcon(r.type)} ${esc(r.name)} ${r.favorite?'⭐':''} ${r.emergency?'🌱':''}</b><small>${esc([r.type,r.durationMin&&`${r.durationMin} min`,r.difficulty].filter(Boolean).join(' • '))}</small>${list(r.steps).length?`<ol class="sm-motion-steps">${list(r.steps).slice(0,5).map(s=>`<li>${esc(s)}</li>`).join('')}${r.steps.length>5?`<li>+ ${r.steps.length-5} more</li>`:''}</ol>`:''}</div><div class="sm-motion-actions"><button class="primary" data-run-routine="${esc(r.id)}">▶ Start</button><button data-plan-routine="${esc(r.id)}">Today</button><button data-log-routine="${esc(r.id)}">Log</button><button data-edit-routine="${esc(r.id)}">✎</button><button data-delete-routine="${esc(r.id)}">×</button></div></article>`).join('')||'<div class="sm-motion-empty">No movement routines yet. Add Pilates, treadmill, stretching, anything that belongs to your body. 🌷</div>';
  const videoRows=m.videos.map(v=>{const url=safeUrl(v.url);return `<article class="sm-motion-row"><div><b>🎥 ${esc(v.title)} ${v.favorite?'⭐':''}</b><small>${esc([v.creator,v.type,v.durationMin&&`${v.durationMin} min`,v.difficulty,v.status].filter(Boolean).join(' • '))}</small>${v.notes?`<small>${esc(v.notes)}</small>`:''}<small class="sm-motion-video-link">${esc(v.url||'')}</small></div><div class="sm-motion-actions">${url?`<a href="${esc(url)}" target="_blank" rel="noopener noreferrer">Open ↗</a>`:''}<button data-plan-video="${esc(v.id)}">Today</button><button data-log-video="${esc(v.id)}">Log</button><button data-edit-video="${esc(v.id)}">✎</button><button data-delete-video="${esc(v.id)}">×</button></div></article>`}).join('')||'<div class="sm-motion-empty">Your future Pilates video stash is waiting. 🎥</div>';
  const sessionRows=[...m.sessions].sort((a,b)=>String(b.date).localeCompare(String(a.date))||String(b.time||'').localeCompare(String(a.time||''))).slice(0,20).map(s=>`<article class="sm-motion-row"><div><b>${typeIcon(s.type)} ${esc(s.title||s.type||'Movement')}</b><small>${esc([prettyDate(s.date),s.time,s.durationMin&&`${s.durationMin} min`,s.distanceMi&&`${s.distanceMi} mi`,s.speed&&`speed ${s.speed}`,s.incline&&`incline ${s.incline}`,s.feel].filter(Boolean).join(' • '))}</small>${s.bodyNotes?`<small>Body: ${esc(s.bodyNotes)}</small>`:''}${s.notes?`<small>${esc(s.notes)}</small>`:''}</div><div class="sm-motion-actions"><button data-edit-session="${esc(s.id)}">✎</button><button data-delete-session="${esc(s.id)}">×</button></div></article>`).join('')||'<div class="sm-motion-empty">No sessions logged yet. Tiny movement still deserves a line in the story. 🌷</div>';
  const weightRows=latestWeights.slice(0,10).map(w=>`<article class="sm-motion-row"><div><b>⚖️ ${esc(w.weightLb)} lb</b><small>${esc(prettyDate(w.date))}${w.note?` • ${esc(w.note)}`:''}</small></div><div class="sm-motion-actions"><button data-edit-weight="${esc(w.id)}">✎</button><button data-delete-weight="${esc(w.id)}">×</button></div></article>`).join('')||'<div class="sm-motion-empty">No weigh-ins yet. Sundays can become your gentle checkpoint. ⚖️</div>';
  root.innerHTML=`<section class="v17-card sm-motion"><section class="sm-motion-hero"><div class="v17-eyebrow">🍓 KATOS • MOVEMENT</div><h1>🌷 Motion Meadow</h1><p>Movement counts even when it is tiny. Plan it, save it, do it, or simply notice that your body moved today.</p></section><div class="sm-motion-grid"><div class="sm-motion-stat"><small>THIS WEEK</small><strong>${weekSessions.length} session${weekSessions.length===1?'':'s'}</strong></div><div class="sm-motion-stat"><small>MINUTES MOVED</small><strong>${weekMinutes}</strong></div><div class="sm-motion-stat"><small>PILATES</small><strong>${pilates}</strong></div><div class="sm-motion-stat"><small>TREADMILL MILES</small><strong>${Math.round(treadmillMiles*100)/100}</strong></div></div><section class="sm-motion-card sm-motion-today"><header><div><small>🌸 TODAY'S MOVEMENT</small><h2>${todayPlans.length?'Your meadow has a plan':'What sounds doable?'}</h2></div><div class="sm-motion-actions"><button class="primary" data-open="plan">＋ Plan</button><button data-open="session">＋ Log movement</button></div></header>${planRows}${(favorites.length||emergency.length)?`<div style="margin-top:12px"><small>QUICK PICKS</small><div class="sm-motion-faves">${[...new Map([...emergency,...favorites].map(r=>[r.id,r])).values()].slice(0,6).map(r=>`<button class="sm-motion-fave" data-plan-routine="${esc(r.id)}">${r.emergency?'🌱':'⭐'} ${esc(r.name)}</button>`).join('')}</div></div>`:''}</section><section class="sm-motion-card"><header><div><small>📅 THIS WEEK</small><h2>Weekly View</h2></div></header><div class="sm-motion-week">${weekMarkup}</div></section><section class="sm-motion-card"><header><div><small>⚖️ SUNDAY CHECK-IN</small><h2>${isSunday&&!sundayEntry?'Sunday weigh-in is ready 🌷':latest?`Latest: ${esc(latest.weightLb)} lb`:'A gentle weekly checkpoint'}</h2><small>${weightDelta===null?'No comparison needed yet.':weightDelta===0?'Same as last check-in.':`${weightDelta>0?'+':''}${weightDelta} lb from previous check-in.`}</small></div><button class="primary" data-open="weight">${sundayEntry?'＋ Add / catch up':'⚖️ Log weigh-in'}</button></header><div class="sm-motion-weight"><div class="sm-motion-weight-box ${isSunday&&!sundayEntry?'sm-motion-due':''}"><small>THIS SUNDAY</small><strong>${sundayEntry?`${esc(sundayEntry.weightLb)} lb`:isSunday?'Ready when you are':'Not due yet'}</strong></div><div class="sm-motion-weight-box"><small>LATEST CHECK-IN</small><strong>${latest?`${esc(latest.weightLb)} lb`:'—'}</strong></div></div></section><section class="sm-motion-card"><header><div><small>🧘🏽‍♀️ MY ROUTINES</small><h2>Movement Recipes</h2><small>Reusable Pilates, treadmill, stretching, or whatever works for you.</small></div><button class="primary" data-open="routine">＋ Routine</button></header><div class="sm-motion-list">${routineRows}</div></section><section class="sm-motion-card"><header><div><small>🎥 VIDEO SHELF</small><h2>Future Me Can Press Play</h2><small>Save videos now so you do not have to hunt for them when motivation finally appears.</small></div><button class="primary" data-open="video">＋ Video</button></header><div class="sm-motion-list">${videoRows}</div></section><section class="sm-motion-card"><header><div><small>🏃🏽‍♀️ MOVEMENT LOG</small><h2>What My Body Actually Did</h2><small>Sessions are history, not a streak punishment.</small></div><button class="primary" data-open="session">＋ Log</button></header><div class="sm-motion-list">${sessionRows}</div></section><section class="sm-motion-card"><header><div><small>📈 GENTLE PROGRESS</small><h2>More Than a Scale</h2></div></header><div class="sm-motion-grid"><div class="sm-motion-stat"><small>SESSIONS</small><strong>${weekSessions.length}</strong></div><div class="sm-motion-stat"><small>MINUTES</small><strong>${weekMinutes}</strong></div><div class="sm-motion-stat"><small>PILATES DAYS</small><strong>${pilates}</strong></div><div class="sm-motion-stat"><small>LATEST WEIGHT</small><strong>${latest?esc(latest.weightLb):'—'}</strong></div></div></section><section class="sm-motion-card"><header><div><small>⚖️ WEIGH-IN HISTORY</small><h2>Sunday Check-Ins</h2></div><button data-open="weight">＋ Weigh-in</button></header><div class="sm-motion-list">${weightRows}</div></section>${modalMarkup(m)}</section>`;
  bind();
 };

 const bind=()=>{
  root.querySelectorAll('[data-open]').forEach(b=>b.addEventListener('click',()=>open(b.dataset.open)));
  root.querySelectorAll('[data-close]').forEach(b=>b.addEventListener('click',closeModal));
  root.querySelector('[data-motion-modal]')?.addEventListener('click',e=>{if(e.target.matches('[data-motion-modal]'))closeModal()});
  root.querySelectorAll('[data-edit-routine]').forEach(b=>b.addEventListener('click',()=>open('routine',b.dataset.editRoutine)));
  root.querySelectorAll('[data-edit-video]').forEach(b=>b.addEventListener('click',()=>open('video',b.dataset.editVideo)));
  root.querySelectorAll('[data-edit-session]').forEach(b=>b.addEventListener('click',()=>open('session',b.dataset.editSession)));
  root.querySelectorAll('[data-edit-weight]').forEach(b=>b.addEventListener('click',()=>open('weight',b.dataset.editWeight)));
  root.querySelectorAll('[data-delete-routine]').forEach(b=>b.addEventListener('click',()=>deleteItem('routines',b.dataset.deleteRoutine)));
  root.querySelectorAll('[data-delete-video]').forEach(b=>b.addEventListener('click',()=>deleteItem('videos',b.dataset.deleteVideo)));
  root.querySelectorAll('[data-delete-session]').forEach(b=>b.addEventListener('click',()=>deleteItem('sessions',b.dataset.deleteSession)));
  root.querySelectorAll('[data-delete-weight]').forEach(b=>b.addEventListener('click',()=>deleteItem('weighIns',b.dataset.deleteWeight)));
  root.querySelectorAll('[data-delete-plan]').forEach(b=>b.addEventListener('click',()=>deleteItem('plans',b.dataset.deletePlan)));
  root.querySelectorAll('[data-plan-routine]').forEach(b=>b.addEventListener('click',()=>{const r=get().routines.find(x=>String(x.id)===String(b.dataset.planRoutine));if(r)addPlan('routine',r.id,r.name)}));
  root.querySelectorAll('[data-plan-video]').forEach(b=>b.addEventListener('click',()=>{const v=get().videos.find(x=>String(x.id)===String(b.dataset.planVideo));if(v)addPlan('video',v.id,v.title)}));
  root.querySelectorAll('[data-run-routine]').forEach(b=>b.addEventListener('click',()=>open('runner',b.dataset.runRoutine)));
  root.querySelectorAll('[data-log-routine]').forEach(b=>b.addEventListener('click',()=>{const r=get().routines.find(x=>String(x.id)===String(b.dataset.logRoutine));if(r){window.__smMotionPrefill=sessionFromSource('routine',r);open('session')}}));
  root.querySelectorAll('[data-log-video]').forEach(b=>b.addEventListener('click',()=>{const v=get().videos.find(x=>String(x.id)===String(b.dataset.logVideo));if(v){window.__smMotionPrefill=sessionFromSource('video',v);open('session')}}));
  root.querySelectorAll('[data-plan-skip]').forEach(b=>b.addEventListener('click',()=>updatePlan(b.dataset.planSkip,{status:'skipped'})));
  root.querySelectorAll('[data-plan-reset]').forEach(b=>b.addEventListener('click',()=>updatePlan(b.dataset.planReset,{status:'planned'})));
  root.querySelectorAll('[data-plan-task]').forEach(b=>b.addEventListener('click',()=>{const p=get().plans.find(x=>String(x.id)===String(b.dataset.planTask));if(p)addToTasks(p)}));
  root.querySelectorAll('[data-plan-log]').forEach(b=>b.addEventListener('click',()=>{const m=get(),p=m.plans.find(x=>String(x.id)===String(b.dataset.planLog));if(!p)return;const source=linkedSource(m,p.sourceType,p.sourceId);window.__smMotionPrefill={...sessionFromSource(p.sourceType,source),title:planLabel(m,p),planId:p.id};open('session')}));
  root.querySelectorAll('[data-run-step]').forEach(c=>c.addEventListener('change',()=>{runChecks[Number(c.dataset.runStep)]=c.checked;draw()}));
  root.querySelector('[data-finish-run]')?.addEventListener('click',()=>{const r=get().routines.find(x=>String(x.id)===String(editingId));if(!r)return;window.__smMotionPrefill=sessionFromSource('routine',r);modal='session';editingId=null;runChecks=[];draw()});
  root.querySelector('[data-form="routine"]')?.addEventListener('submit',e=>{e.preventDefault();const f=new FormData(e.currentTarget),m=get(),item={id:editingId||id('move-routine'),name:String(f.get('name')||'').trim(),type:String(f.get('type')||'Pilates'),durationMin:num(f.get('durationMin'),0),difficulty:String(f.get('difficulty')||''),steps:String(f.get('steps')||'').split('\n').map(x=>x.trim()).filter(Boolean),notes:String(f.get('notes')||'').trim(),favorite:f.get('favorite')==='on',emergency:f.get('emergency')==='on',updatedAt:new Date().toISOString()};if(!item.name)return;saveMotion({routines:editingId?m.routines.map(x=>String(x.id)===String(editingId)?{...x,...item}:x):[...m.routines,{...item,createdAt:new Date().toISOString()}]});closeModal()});
  root.querySelector('[data-form="video"]')?.addEventListener('submit',e=>{e.preventDefault();const f=new FormData(e.currentTarget),url=String(f.get('url')||'').trim();if(!safeUrl(url))return alert('Please use a normal http or https video link.');const m=get(),item={id:editingId||id('move-video'),title:String(f.get('title')||'').trim(),url,creator:String(f.get('creator')||'').trim(),type:String(f.get('type')||'Pilates'),durationMin:num(f.get('durationMin'),0),difficulty:String(f.get('difficulty')||''),status:String(f.get('status')||'Want to Try'),favorite:f.get('favorite')==='on',notes:String(f.get('notes')||'').trim(),updatedAt:new Date().toISOString()};if(!item.title)return;saveMotion({videos:editingId?m.videos.map(x=>String(x.id)===String(editingId)?{...x,...item}:x):[...m.videos,{...item,createdAt:new Date().toISOString()}]});closeModal()});
  root.querySelector('[data-form="session"]')?.addEventListener('submit',e=>{e.preventDefault();const f=new FormData(e.currentTarget),m=get(),item={id:editingId||id('move-session'),date:String(f.get('date')||today()),time:String(f.get('time')||''),type:String(f.get('type')||'Other'),title:String(f.get('title')||'').trim(),durationMin:num(f.get('durationMin'),0),distanceMi:num(f.get('distanceMi'),0),speed:num(f.get('speed'),0),incline:num(f.get('incline'),0),feel:String(f.get('feel')||''),bodyNotes:String(f.get('bodyNotes')||'').trim(),notes:String(f.get('notes')||'').trim(),sourceType:String(f.get('sourceType')||''),sourceId:String(f.get('sourceId')||'')||null,planId:String(f.get('planId')||'')||null,updatedAt:new Date().toISOString()};saveMotion({sessions:editingId?m.sessions.map(x=>String(x.id)===String(editingId)?{...x,...item}:x):[...m.sessions,{...item,createdAt:new Date().toISOString()}],plans:item.planId?m.plans.map(p=>String(p.id)===String(item.planId)?{...p,status:'complete',completedAt:new Date().toISOString()}:p):m.plans});closeModal()});
  root.querySelector('[data-form="plan"]')?.addEventListener('submit',e=>{e.preventDefault();const f=new FormData(e.currentTarget),label=String(f.get('label')||'').trim(),date=String(f.get('date')||today());if(!label)return;const m=get(),plan={id:id('moveplan'),date,sourceType:'custom',sourceId:null,label,status:'planned',createdAt:new Date().toISOString()};saveMotion({plans:[...m.plans,plan]});if(f.get('task')==='on')addToTasks(plan);closeModal()});
  root.querySelector('[data-form="weight"]')?.addEventListener('submit',e=>{e.preventDefault();const f=new FormData(e.currentTarget),m=get(),weight=num(f.get('weightLb'),0);if(!weight)return;const item={id:editingId||id('weigh'),date:String(f.get('date')||sundayKey()),weightLb:weight,note:String(f.get('note')||'').trim(),updatedAt:new Date().toISOString()};saveMotion({weighIns:editingId?m.weighIns.map(x=>String(x.id)===String(editingId)?{...x,...item}:x):[...m.weighIns,{...item,createdAt:new Date().toISOString()}]});closeModal()});
 };
 store.subscribe(draw);draw();
}

export{renderMotion};
