const id=()=>`gig-${Date.now().toString(36)}-${Math.random().toString(36).slice(2,8)}`;const dateKey=d=>new Date(d).toISOString().slice(0,10);const monday=d=>{const x=new Date(`${dateKey(d)}T12:00:00`),offset=(x.getDay()+6)%7;x.setDate(x.getDate()-offset);return dateKey(x)};export function addGigEntry(store,d={}){const earnings=Number(d.earnings)||0;if(!d.platform||earnings<0)return{ok:false};const item={id:id(),platform:String(d.platform),date:String(d.date||dateKey(new Date())),earnings,tips:Number(d.tips)||0,hours:Number(d.hours)||0,miles:Number(d.miles)||0,note:String(d.note||'')};store.update(s=>({...s,gigWork:[...(Array.isArray(s.gigWork)?s.gigWork:[]),item]}));return{ok:true,item}}export const gigTotal=(data={},reference=new Date())=>{const start=monday(reference),end=new Date(`${start}T12:00:00`);end.setDate(end.getDate()+7);const finish=dateKey(end);return(data.gigWork||[]).filter(x=>String(x.date)>=start&&String(x.date)<finish).reduce((n,x)=>n+Number(x.earnings||0)+Number(x.tips||0),0)};