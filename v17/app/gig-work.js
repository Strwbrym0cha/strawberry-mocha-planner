const id=()=>`gig-${Date.now().toString(36)}-${Math.random().toString(36).slice(2,8)}`;export function addGigEntry(store,d={}){const earnings=Number(d.earnings)||0;if(!d.platform||earnings<0)return{ok:false};const item={id:id(),platform:String(d.platform),date:String(d.date||new Date().toISOString().slice(0,10)),earnings,tips:Number(d.tips)||0,hours:Number(d.hours)||0,miles:Number(d.miles)||0,note:String(d.note||'')};store.update(s=>({...s,gigWork:[...(Array.isArray(s.gigWork)?s.gigWork:[]),item]}));return{ok:true,item}}export const gigTotal=(data={},week='')=>(data.gigWork||[]).filter(x=>!week||String(x.date).startsWith(week)).reduce((n,x)=>n+Number(x.earnings||0)+Number(x.tips||0),0);