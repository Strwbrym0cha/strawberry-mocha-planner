import {loadLocalData,saveLocalData} from './data.js';export function createStore(){let data=loadLocalData();const listeners=new Set();return{get(){return data},set(next){data=next;saveLocalData(data);listeners.forEach(fn=>fn(data))},update(fn){data=fn(data)||data;saveLocalData(data);listeners.forEach(fn=>fn(data))},subscribe(fn){listeners.add(fn);return()=>listeners.delete(fn)},reload(){data=loadLocalData();listeners.forEach(fn=>fn(data))}}}export async function cloudSync(){return{ok:true,migrated:false}}