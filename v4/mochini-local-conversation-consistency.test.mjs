import assert from'node:assert/strict';import{processMochini}from'./mochini.js';import{seedCanonicalLore,normalizeMochiniLore}from'./mochini-lore.js';
const lore=seedCanonicalLore({}),state=()=>({mochini:{conversation:[],pendingProposal:null,life:{mood:'content',energy:70,currentActivity:'organizing her spoons',currentObsession:'worms'},lore},life:{tasks:[],reminders:[],events:[],routines:[],routineInstances:[]},v4:{brainDump:[]},context:{}});
for(const q of ['Hi Mochini whatcha doin?','Whatcha doinnn','what you up to']){const r=processMochini(state(),q);assert.equal(r.requiresAI,false);assert.equal(r.route,'local');assert.match(r.reply,/organizing her spoons/i)};assert.equal(processMochini(state(),'wyd').requiresAI,false)
const before=JSON.stringify(state().mochini.life);processMochini(state(),'what are you doing');assert.equal(JSON.stringify(state().mochini.life),before);
for(const q of ["What's up with all the spoons?",'What happened to the spoons?','Why do you have so many spoons?','Tell me about your spoons']){const r=processMochini(state(),q);assert.equal(r.requiresAI,false);assert.doesNotMatch(r.reply,/no spoons are currently documented/i)}
const noFurniture=state();noFurniture.mochini.lore=normalizeMochiniLore({canonSeedVersion:1,hoard:{...lore.hoard,tinyFurniture:[]}});assert.match(processMochini(noFurniture,'Where are all these chairs coming from?').reply,/no furniture|Bean Enterprises/i);assert.equal(processMochini(state(),'Can you help me plan tomorrow?').route,'ai');assert.equal(processMochini(state(),'Add take out trash tomorrow').state.mochini.pendingProposal.kind,'task');for(const q of ['Stop taking my fries','My day was weird'])assert.equal(processMochini(state(),q).route,'ai');console.log('V4 local conversation consistency tests: PASS');
