import assert from'node:assert/strict';import{normalizeLife}from'./mochini-life.js';
const a=new Date('2026-08-23T23:30:00-05:00'),b=new Date('2026-08-24T00:30:00-05:00');const same=normalizeLife({dailyKey:'2026-08-23',weeklyKey:'2026-W34',interactionsToday:2,berriesFedToday:3},a);assert.equal(same.interactionsToday,2);assert.equal(same.berriesFedToday,3);const next=normalizeLife(same,b);assert.equal(next.interactionsToday,0);assert.equal(next.berriesFedToday,0);assert.equal(normalizeLife({dailyKey:'bad',weeklyKey:'bad'},a).interactionsToday,0);console.log('V4 Mochini Life date boundary tests: PASS');
