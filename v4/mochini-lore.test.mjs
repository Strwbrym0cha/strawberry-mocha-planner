import assert from'node:assert/strict';import{addHoardItem,getHoardSummary,getLoreResponse,normalizeMochiniLore,resolveScandal,setEra,startScandal}from'./mochini-lore.js';import{processMochini}from'./mochini.js';
let lore=normalizeMochiniLore({});assert.equal(getHoardSummary(lore).length,0);const item={id:'chair-1',name:'tiny chair'};lore=addHoardItem(lore,'tinyFurniture',item);lore=addHoardItem(lore,'tinyFurniture',item);assert.equal(lore.hoard.tinyFurniture.length,1);assert.equal(getLoreResponse(lore,{},'What do you have in your corner?').includes('1'),true);assert.equal(getLoreResponse(lore,{},'How many chairs do you have?').includes('tiny chair'),true);
lore=startScandal(lore,{id:'berry',name:'The Strawberry Incident',summary:'Berry accounting remains unclear',mochiniStatement:'I relocated it into my stomach for safekeeping.'});assert.equal(lore.activeScandals.length,1);lore=resolveScandal(lore,'berry');assert.equal(lore.resolvedScandals.length,1);lore=setEra(lore,'Worm Era');assert.equal(lore.era,'Worm Era');const before=JSON.stringify(lore);getLoreResponse(lore,{},'What is Bean Enterprises?');assert.equal(JSON.stringify(lore),before);
const state={mochini:{conversation:[],pendingProposal:null,life:{mood:'content',energy:70,currentObsession:'worm',currentActivity:'organizing tiny objects'},lore},life:{tasks:[],reminders:[],events:[],routines:[],routineInstances:[]},v4:{brainDump:[]},context:{}};for(const q of ['What is Bean Enterprises?','Why do you hate clocks?','What happened with the strawberries?','What are you obsessed with?']){const r=processMochini(state,q);assert.equal(r.route,'local');assert.equal(r.requiresAI,false)}for(const q of ['Can you help me plan tomorrow?','Help me decide what to work on'])assert.equal(processMochini(state,q).route,'ai');assert.equal(processMochini(state,'Add a task to call Mom').state.life.tasks.length,0);console.log('V4 Mochini Lore tests: PASS');
